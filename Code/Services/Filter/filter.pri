# 掩码与过滤规则（FILT-001）。
#
# 本模块刻意分成两部分：
#   mask.{h,cpp}        —— 掩码语言本身：语法、解析、匹配、语法速查（平台无关）
#   maskfilter.{h,cpp}  —— 过滤声明：包含/排除如何叠加、大小写策略、预览计数（平台无关）
#
# 为什么两层要分开：`mask.h` 回答「这一段掩码命中这个条目吗」，`maskfilter.h`
# 回答「整个声明叠加之后这个条目留不留」。两者的失败也是两类——前者是「你的掩码
# 写错了」（界面要在输入框里标红那一段），后者是「你的规则组合得不对」
# （界面要在状态栏说明排除优先）。返回值合成一个之后，界面就没法分别显示。
#
# 本模块只依赖 QtCore：输入输出都是字符串，不碰文件系统、不碰图形、不碰平台 API。
# 因此**三种平台的大小写规则都能在 macOS 上被真实执行**——这正是 FILT-001 第 3 条
# 完成标准要的东西。「Windows 默认不敏感」这条规则如果写成 `#ifdef Q_OS_WIN`，
# 在开发机上就一次都不会执行到。
INCLUDEPATH += $$PWD

HEADERS += \
    $$PWD/mask.h \
    $$PWD/maskfilter.h

SOURCES += \
    $$PWD/mask.cpp \
    $$PWD/maskfilter.cpp
