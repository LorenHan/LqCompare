# 测试共享支撑模块。
#
# 这里放「多个测试套件都要用的测试替身」。目前是内存文件系统，
# 后续文件夹比对（工作流 E）、同步（J）等测试都要靠它来制造权限不足、
# 被占用这类在真实文件系统上极难稳定复现的异常路径。
#
# 它刻意不是某个套件的私有文件：一旦变成私有，下一个人需要时就会
# 复制一份，然后两份各自演化——这正是护栏类代码最容易发生的腐化。
INCLUDEPATH += $$PWD

HEADERS += $$PWD/fakefilesystem.h
SOURCES += $$PWD/fakefilesystem.cpp
