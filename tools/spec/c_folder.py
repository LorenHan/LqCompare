# -*- coding: utf-8 -*-
"""文件夹比对（DIR）、文件夹同步（SYNC）、文件夹合并（FMG）规格。"""

from . import A

ACTIONS = [
    # ------------------------------------------------------------------
    # DIR —— 文件夹比对
    # ------------------------------------------------------------------
    A("DIR-001", "文件夹比对", "文件夹比对会话与双树视图",
      "入口：新建文件夹比较会话 / 拖入两个文件夹 / 命令行传两个目录。\n"
      "作用对象：左右两个文件夹树的并排视图。\n"
      "边界：文件夹比对是主视图——文件比对是它的子视图；"
      "双击文件在标签内打开子视图而不是另开主窗口。",
      ["打开两个文件夹后显示并排双栏树，含列头、状态图标、差异着色",
       "双击文件在标签内的子视图中打开对应比对，返回时回到文件夹列表原位置",
       "双击子文件夹在该窗格内导航进入，并可用「向上一级」返回",
       "两侧文件夹路径显示在 Compare 页 Sources 组并可直接修改",
       "文件夹路径相同或互为子目录时给出提示（避免自我比较导致无限递归）"],
      "BC5 §9（文件夹视图）+ TGIT §D（TortoiseGit 无文件夹比对，需自建信息架构）"),

    A("DIR-002", "文件夹比对", "目录扫描引擎与并行扫描",
      "入口：打开文件夹会话后自动扫描。\n"
      "作用对象：两个目录树的条目枚举与配对。\n"
      "边界：扫描必须可取消；取消后已扫描部分保持可用而不是整体丢弃。",
      ["扫描在后台线程执行，界面保持响应且状态栏显示进度（已扫描条目数/当前路径）",
       "支持多线程并行扫描（按子目录并行），线程数可配置",
       "提供取消按钮，取消后已得结果可用且状态栏标注「扫描已取消，结果不完整」",
       "扫描过程中遇到无权限目录不中断整体扫描，记录为错误条目",
       "同一目录在文件系统未变化时扫描结果一致（确定性）"],
      "BC5 §8.3（文件夹处理）+ TGIT §C.3 C044（后台扫描）"),

    A("DIR-003", "文件夹比对", "递归子目录策略",
      "入口：Compare 页 Folder Options 组的「比较子文件夹」。\n"
      "作用对象：子目录是否参与比较及递归深度。\n"
      "边界：不递归时扫描根目录直属条目、显示子目录为未扫描；限制深度 1 时再扫描直接子目录内部直属条目；完全递归遍历全部子树，三者不得仅改变显示。",
      ["三档：根目录直属条目（不进入子目录）、递归深度 1（进入直接子目录）、完全递归",
       "深度边界上的子目录节点仍显示且标记未扫描；内部未枚举内容不得计为相同或不存在",
       "切换档位触发重新扫描（或对已扫描结果重新过滤，取性能更优者）",
       "递归深度可另设上限以避免超深目录树，达到上限时明确提示",
       "循环符号链接导致的无限递归被检测并终止，记录为错误条目"],
      "BC5 §8.3（Folder handling）+ TGIT §C.3 C043"),

    A("DIR-004", "文件夹比对", "快速测试：文件大小",
      "入口：Compare 页 Folder Options 组的「比较文件大小」。\n"
      "作用对象：文件对的快速相等判定。\n"
      "边界：大小不同可证明字节不同，但不能证明规则不同（如 CRLF 与 LF）。启用规则比较或内容覆盖快速测试时不得因大小不同跳过内容计算。",
      ["大小为快速测试的一项，可单独开关",
       "大小不同记录「大小不同」；仅在字节相等判定场景可安全短路，规则比较仍须执行并分别记录字节与规则结论",
       "大小相同但内容比对未启用时状态为「未知（未比对内容）」而不是「相同」",
       "状态语义在界面与报表中一致，不把未知当成相同"],
      "BC5 §8.1（快速测试）+ §8.2"),

    A("DIR-005", "文件夹比对", "快速测试：修改时间",
      "入口：Compare 页 Folder Options 组的「比较时间戳」。\n"
      "作用对象：文件修改时间差的判定。\n"
      "边界：必须处理时间精度差异（FAT 2 秒 / NTFS 100ns / 不同文件系统）"
      "与时区、夏令时，否则会产生大量虚假差异。",
      ["可开关时间比较；开启时可配置时间容差（默认 2 秒以兼容 FAT 精度）",
       "默认比较 UTC 时间戳而不是本地显示时间，避免时区/夏令时误判",
       "提供「忽略时间，只比大小与内容」的快捷组合预设",
       "时间比较结果在列中可见（含差值显示），并支持按差值排序",
       "跨时区挂载（网络盘、虚拟机共享）下不产生系统性虚假差异（有测试）"],
      "BC5 §8.1（时间比较与容差）+ TGIT §C.3 C046"),

    A("DIR-006", "文件夹比对", "快速测试：文件名大小写与规范化",
      "入口：Compare 页 Folder Options 组的「比较文件名大小写」。\n"
      "作用对象：文件名相等判定。\n"
      "边界：必须区分「文件系统本身大小写敏感」（如 Linux/ext4）与"
      "「仅比较时忽略大小写」两种语义，二者不可混淆。",
      ["提供「比较文件名大小写」开关；关闭时忽略大小写匹配同名文件",
       "文件系统大小写规则只决定路径身份与可操作性，不覆盖用户的比较设置；不区分大小写配对遇一对多碰撞时报告歧义，不静默取首项",
       "大小写仅不同的文件对在忽略大小写模式下配成一对，并在列中提示「仅大小写不同」",
       "提供一键「统一为左侧大小写」（重命名操作）并纳入撤销范围"],
      "BC5 §8.3 + TGIT §C.3 C050"),

    A("DIR-007", "文件夹比对", "内容比对：CRC 校验和",
      "入口：Compare 页 Folder Options 组的「比较内容」下拉 → CRC。\n"
      "作用对象：文件内容相等的快速判定。\n"
      "边界：CRC 相等视为相同（有极低碰撞概率，需在文档与界面注明）；"
      "CRC 计算必须可缓存，避免重复扫描重复计算。",
      ["支持 CRC32 比对，计算在后台线程池并行执行并显示进度",
       "CRC 结果缓存到内存并带文件键（路径+大小+修改时间+所在文件系统 ID），命中缓存时跳过重算",
       "提供「CRC 与逐字节双重校验」可选项，用于关键场景消除碰撞风险",
       "CRC 列可显示、排序、导出到报表",
       "对同一批文件重复比较时第二次明显更快（有性能测试）",
       "缓存命中仅为快速复用；提供强制重算内容入口以应对大小/时间戳未变的改写，比对前后验证文件身份与元数据，变化时结果标为不确定"],
      "BC5 §8.2（内容比对）+ §9.1（列中的 CRC）"),

    A("DIR-008", "文件夹比对", "内容比对：二进制逐字节",
      "入口：Compare 页 Folder Options 组的「比较内容」下拉 → 二进制。\n"
      "作用对象：文件字节级相等判定。\n"
      "边界：必须支持「只比对前 N 字节」的快速模式，且明确标注为不完整比对。",
      ["逐字节比对两文件，遇首个不同字节即可提前判定不同（不读完整文件）",
       "提供「只比较前 N 字节」选项（默认关闭），开启时状态标注「部分比较」",
       "大文件比对不一次性读入内存，采用分块读取并对分块大小有上界",
       "比对结果记录首个差异的偏移量，报表中可查看",
       "文件在比对过程中被修改时检测并标记为「比对期间文件变化」而不是给出错误结论"],
      "BC5 §8.2（二进制比对）+ §18（十六进制比对）"),

    A("DIR-009", "文件夹比对", "内容比对：基于文件格式规则",
      "入口：Compare 页 Folder Options 组的「比较内容」下拉 → 基于规则。\n"
      "作用对象：按文件格式定义的规则做语义化比对。\n"
      "边界：必须复用文件格式定义与文本比对的忽略规则，"
      "不允许为文件夹比对另写一套规则解释器。",
      ["按文件格式定义对每个文件对选择对应的规则化比对（含忽略空白/大小写/注释等）",
       "未命中格式定义时回退到 CRC 或二进制比对，且状态栏说明所用方式",
       "规则化比对的结果与在文本比对会话中打开同一对文件的结论一致（有交叉测试）",
       "规则化比对较慢，提供进度与取消，并在状态栏显示「正在做规则化比对」"],
      "BC5 §8.2 + §14（文件格式定义）"),

    A("DIR-010", "文件夹比对", "比较准则的组合与优先级",
      "入口：会话设置 → 比较（Comparison）Tab。\n"
      "作用对象：大小 / 时间 / 属性 / 内容 四类准则的启用与顺序。\n"
      "边界：快速测试与内容测试分别记录证据；执行顺序用于性能优化，不应暗中改变结论。是否允许内容结果覆盖快速测试差异由显式选项决定。",
      ["大小、时间戳、属性快速测试可分别开关；内容方式在 CRC/二进制/基于规则之间选择，执行顺序由引擎依赖约束决定",
       "提供快速（大小+时间）、标准（CRC）、严格字节（二进制）、规则比较预设；快速匹配必须标明未验证内容",
       "提供「内容结果覆盖快速测试」与「快速测试匹配时跳过内容」两个显式选项；前者开启时内容相同可覆盖时间等差异，后者跳过时内容仍为未知",
       "规则比较不能因原始大小不同提前终止；仅当结果不可能被后续准则改变时允许短路，并以 CRLF/LF、空白与时间戳语料覆盖",
       "单文件临时准则覆盖不影响会话默认值；最终判定及每项证据在详情和报表中可见"],
      "BC5 §8；官方 https://www.scootersoftware.com/v5help/sessiondircomparison.html"),

    A("DIR-011", "文件夹比对", "条目状态判定与语义",
      "入口：扫描完成后由比对引擎产出。\n"
      "作用对象：每个条目的状态分类。\n"
      "边界：主比较状态互斥；存在性、内容结论、时间关系与扫描完整性分别存储。两侧均改只能由同步/合并基线推导，不可凭两份当前文件推断。",
      ["主状态包含相同、不同、仅左、仅右、错误、未知；内容证据另区分字节相同/规则相同/CRC相同/未比较/部分比较",
       "左侧较新/右侧较新/同时间为独立时间维度；孤儿项为仅左和仅右的显示集合，不再另作互斥状态",
       "只有获得有效基线时才显示左右均改及冲突；无权限、扫描取消、内容未读完不得归为相同或不存在",
       "右键「为什么是这个状态」列出各准则、覆盖策略与最终结论；颜色之外同时提供图标和文本",
       "固定数据源与准则下父子视图结论一致，状态组合有真值表测试"],
      "BC5 §9.1（列与排序 / 状态）+ §8.2"),

    A("DIR-012", "文件夹比对", "状态着色与图标",
      "入口：View 页 Coloring 组；文件夹视图的行渲染。\n"
      "作用对象：行的背景色、前景色与左侧图标。\n"
      "边界：着色方案可整体替换；不得因着色影响可读性（对比度要求）。",
      ["每类状态有默认颜色与图标，且图标随主题切换自动适配深浅",
       "提供至少 3 套配色方案（默认/高对比/色盲友好）并可切换",
       "颜色仅作为辅助，状态图标与文字提示必须同时存在（不单靠颜色传达信息）",
       "着色方案切换立即重绘列表，不需要重新扫描",
       "自定义颜色可导出为配色文件并分享"],
      "BC5 §33（外观颜色）+ §9.1"),

    A("DIR-013", "文件夹比对", "列定义与自定义列",
      "入口：文件夹视图列头右键；会话设置中的列配置。\n"
      "作用对象：每行展示的信息字段。\n"
      "边界：列是视图属性，不写入会话文件；"
      "但列集合与顺序应按视图类型记忆。",
      ["内置列：文件名、扩展名、左侧大小、右侧大小、左侧修改时间、右侧修改时间、状态、CRC、版本、所有者/组、属性、差异摘要",
       "可显示/隐藏列、调整宽度与顺序，配置按视图类型持久化",
       "提供「恢复默认列」命令",
       "宽度自适应命令（按内容或按列头）可用",
       "列头右键菜单可直接进入列配置"],
      "BC5 §9.1（列与排序）"),

    A("DIR-014", "文件夹比对", "排序、分组与稳定顺序",
      "入口：点击列头排序；View 页排序相关项。\n"
      "作用对象：条目显示顺序。\n"
      "边界：排序必须稳定（相同键保持原顺序），"
      "且「文件夹优先」与「混合排序」两种口径可选。",
      ["点击列头升/降序排序，支持多列组合排序（Shift+点击追加）",
       "提供「文件夹排在文件之前」与「文件夹与文件混合排序」两种模式",
       "排序键对缺失值（仅左/仅右条目）有确定处理并在界面可见（空白而不是 0）",
       "排序稳定：相同键的相对顺序在多次排序后一致",
       "提供「按路径自然序」排序（file2 在 file10 之前）"],
      "BC5 §9.1（排序）+ §32（选项中的排序行为）"),

    A("DIR-015", "文件夹比对", "树展开、折叠与导航",
      "入口：文件夹视图内的展开箭头、快捷键、View 页命令。\n"
      "作用对象：子文件夹节点的展开状态。\n"
      "边界：展开状态在重新扫描后应尽量保持（按路径记忆），"
      "不因刷新而全部折叠。",
      ["可展开/折叠单个节点，支持全部展开、全部折叠、展开到第一层",
       "提供「展开有差异的子文件夹」命令",
       "左右两侧展开状态可联动（默认联动，可关闭）",
       "导航键：上下移动、左右折叠展开、Home/End、类型前缀跳转、PageUp/PageDown",
       "刷新（重新扫描）后按路径恢复此前的展开状态"],
      "BC5 §9.2（展开/折叠与导航）+ TGIT §C.3 C045"),

    A("DIR-016", "文件夹比对", "加载时的展开策略",
      "入口：Compare 页 Folder Options 组的「加载时展开子文件夹」下拉。\n"
      "作用对象：打开会话后的初始展开状态。\n"
      "边界：策略只影响初始状态，之后用户操作优先。",
      ["提供三档：不展开、仅展开有差异的、展开全部（受递归深度限制）",
       "「仅展开有差异的」需要扫描完成后二次遍历，过程中界面可用并有进度",
       "超大目录（>10 万条目）下强制使用「不展开」并提示原因",
       "策略写入会话设置并可通过命令行指定"],
      "BC5 §8.3 + TGIT §C.3 C045"),

    A("DIR-017", "文件夹比对", "显示过滤器（文件夹视图）",
      "入口：View 页 Display Filters 组。\n"
      "作用对象：哪些条目被显示。\n"
      "边界：过滤只影响显示与导航，不影响选择集合的语义（"
      "对隐藏条目的批量操作必须明确是否包含隐藏项）。",
      ["提供：只显示差异、只显示相同、只显示孤儿项、只显示错误、只显示冲突 等过滤器",
       "提供「隐藏被掩码排除的文件」与「隐藏空文件夹」开关",
       "批量操作遇显示过滤隐藏项时默认仅处理当前可见选择，并依 FILT-006 显式显示排除数量/包含隐藏项选项；扫描范围排除项不能通过该选项越界纳入",
       "状态栏显示「可见 N / 共 M」",
       "一键重置全部过滤器"],
      "BC5 §9.3（显示过滤器）+ TGIT §C.3 Page 5"),

    A("DIR-018", "文件夹比对", "选择模型与按状态选择",
      "入口：鼠标选择、键盘选择、右键「选择」子菜单。\n"
      "作用对象：当前选择集合。\n"
      "边界：左右两侧各自持有独立选择集合；"
      "跨侧命令（复制到对面）必须明确以哪一侧为源。",
      ["支持单选、Ctrl 多选、Shift 范围选、全选、反向选择、按状态选择（如选中全部仅左文件）",
       "左右选择集合独立维护，选择状态在状态栏显示（左选 N，右选 M）",
       "提供「同步选择」（左右选中同一对条目）开关用于对照审阅",
       "提供「隔离选择」模式：只显示选中项及其上下文",
       "选择集合在排序变化后按条目身份保持（不因顺序变化而错位）"],
      "BC5 §9.4（选择与批量操作）+ TGIT §C.3 C042"),

    A("DIR-019", "文件夹比对", "复制到左 / 复制到右",
      "入口：右键菜单、Tools 页、快捷键、块操作式按钮。\n"
      "作用对象：把选中条目从一侧复制到另一侧。\n"
      "边界：复制是文件系统写操作，必须可预演、可取消、有明确进度与失败清单。",
      ["对选中条目执行左→右或右→左的复制，源与目标明确显示在确认框中",
       "复制前校验目标空间是否充足、目标是否为只读、是否会产生循环复制（目录复制到自身子目录）",
       "提供「复制并覆盖」「仅复制不存在者」「仅源较新时覆盖」三种不同策略；时间相同但内容不同不得静默按较新策略选边",
       "复制过程显示总进度与单文件进度，可取消；取消后已复制文件保留并列出清单",
       "失败（无权限/被占用）的条目不中断整体流程，汇总为失败清单可重试",
       "覆盖前备份目标，单文件先写临时文件再原子替换；取消/失败不留下半写目标，预演后源或目标变化时重新确认该项"],
      "BC5 §10（文件操作：复制/移动）+ TGIT §C.3"),

    A("DIR-020", "文件夹比对", "复制选项（时间戳 / 属性 / 权限）",
      "入口：会话设置 → 复制（Copy）Tab；复制确认框内的选项。\n"
      "作用对象：复制时保留哪些元数据。\n"
      "边界：跨文件系统复制时某些属性无法保留，必须显式说明而不是静默丢弃。",
      ["可选：保留修改时间、保留访问时间、保留文件属性（只读/隐藏/系统）、保留权限与所有者",
       "跨文件系统（如 NTFS → FAT32、ext4 → exFAT）无法保留的项在确认框中预告说明",
       "保留所有者在无权限时降级为保留权限位并记录日志，不静默失败",
       "复制选项可保存为会话默认值",
       "复制完成后校验（大小+可选 CRC）并在报告中标注校验结果"],
      "BC5 §10（属性同步）+ §11（文件夹同步）"),

    A("DIR-021", "文件夹比对", "移动条目",
      "入口：右键菜单的「移动到左侧/右侧」。\n"
      "作用对象：把选中条目移动（而不是复制）到另一侧。\n"
      "边界：移动是跨卷时退化为复制+删除，必须显式告知用户这一点。",
      ["提供移动命令，语义为源侧删除 + 目标侧新增",
       "跨卷（不同盘符/文件系统）移动时检测并提示「将执行复制后删除，大文件耗时较长」",
       "移动目标已存在同名条目时给出合并/覆盖/跳过三选项，不静默覆盖",
       "移动失败时保证源不被删除（先成功复制再删除源）",
       "移动结果计入操作日志；同卷可逆移动支持撤销，跨卷按 DIR-027 明确说明恢复能力与备份条件，不承诺所有移动可撤销"],
      "BC5 §10（移动）+ TGIT §C.3"),

    A("DIR-022", "文件夹比对", "删除条目（回收站优先）",
      "入口：右键菜单的「删除」、Delete 键。\n"
      "作用对象：选中的文件与文件夹。\n"
      "边界：默认走系统回收站；永久删除必须显式选择并二次确认；"
      "工作区/会话根目录不可删除。",
      ["默认删除到系统回收站（Windows 回收站 / macOS 废纸篓 / Linux 遵循 XDG）",
       "提供「永久删除」变体，需二次确认并在确认框红字标明不可恢复",
       "删除前显示将删除的条目数与总大小，且列出前若干条明细",
       "删除在后台执行并显示进度，失败条目汇总为清单",
       "批量删除上限保护：超过阈值（可配置）时额外确认"],
      "BC5 §10（删除）+ 用户要求：破坏性操作必须显式确认且走可逆路径"),

    A("DIR-023", "文件夹比对", "重命名与批量重命名",
      "入口：右键菜单的「重命名」、F2 键。\n"
      "作用对象：单个或多个条目的名称。\n"
      "边界：重命名先校验名称、冲突与批量循环依赖；只改一侧是合法操作，配对关系随之重算，不得通过静默改另一侧来维持配对。",
      ["单选 F2 就地编辑名称；多选时打开批量重命名对话框",
       "批量重命名支持：查找替换、添加前后缀、序号模板、大小写变换、扩展名变更",
       "重命名前后实时预览结果列表，冲突或非法名称就地标红",
       "重命名只改一侧时，另一侧条目变为「仅左/仅右」状态并在视图刷新",
       "重命名后选择集合跟随被重命名条目"],
      "BC5 §10（重命名）+ Ailecium PathRenameService 的批量重命名经验"),

    A("DIR-024", "文件夹比对", "新建文件夹与文件",
      "入口：右键菜单的「新建文件夹」「新建文件」。\n"
      "作用对象：当前窗格的目标目录。\n"
      "边界：新建必须保证绝不覆盖已存在条目（NewOnly 语义）。",
      ["在某一侧或两侧同时新建文件夹/文件，并可指定初始内容（新文件可为空或复制自对面）",
       "名称冲突时给出明确错误并保持输入框焦点，不覆盖已有文件",
       "新建成功后自动选中并在视图中可见（必要时展开父目录）",
       "名称合法性校验复用统一规则（与重命名同一套平台规则）",
       "新建操作因权限失败时给出可操作原因"],
      "Ailecium docs/PRD-actions.md TREE-17（New File / New Folder 与 NewOnly 语义）"),

    A("DIR-025", "文件夹比对", "镜像（单向覆盖整个目录）",
      "入口：Tools 页或右键「镜像到右侧」「镜像到左侧」。\n"
      "作用对象：整棵目录树。\n"
      "边界：镜像会删除目标侧的多余条目，是最危险的文件夹操作，"
      "必须强制预演 + 二次确认 + 默认走回收站。",
      ["执行前生成完整操作清单（新增 N、覆盖 M、删除 K）并强制用户确认",
       "确认框中单独醒目列出「将被删除的 K 个条目」明细与总大小",
       "默认删除走回收站；永久删除需额外显式勾选",
       "执行过程显示全局进度、可暂停/取消，取消后已完成部分保留",
       "执行完成后生成镜像报告（清单 + 校验结果），并支持导出",
       "扫描取消、读取错误、根目录不可达或预演后源/目标变化时禁止按缺失推导删除，须修复并重新预演"],
      "BC5 §10（镜像）+ §11（同步）"),

    A("DIR-026", "文件夹比对", "文件属性与权限查看/编辑",
      "入口：右键菜单的「属性」；Owner/Group 列。\n"
      "作用对象：条目元数据。\n"
      "边界：属性查看必须对两侧并行展示差异，"
      "而不是分别打开两个属性框让用户自己比对。",
      ["属性对话框并排显示左右两侧的属性，差异项高亮",
       "显示通用属性：只读、隐藏、系统、归档、创建/修改/访问时间、大小、占用空间",
       "Windows 显示扩展属性与 ACL 摘要；Unix 显示权限位、owner、group、符号链接目标",
       "允许修改可修改的属性并写回（只读位、时间戳），修改纳入撤销范围",
       "无法读取的属性（权限不足）显示为「不可读」而不是空白"],
      "BC5 §5.2 新增（Unix owner/group/type、Windows 扩展属性）+ TGIT §A.11"),

    A("DIR-027", "文件夹比对", "文件系统操作的可撤销与操作日志",
      "入口：编辑页撤销、Tools 页操作日志。\n"
      "作用对象：全部文件系统写操作（复制/移动/删除/重命名/新建/镜像/同步）。\n"
      "边界：撤销依赖备份与回收站，不能保证的场景必须提前告知"
      "（如大文件移动、跨卷、永久删除）。",
      ["维护本次会话的文件系统操作日志，支持按批撤销最近一批操作",
       "撤销通过把新副本删除到回收站、把回收站中的旧副本还原实现",
       "对不可撤销的操作（永久删除、跨卷移动）在操作前明确标注「不可撤销」",
       "操作日志可导出为文本，含时间、动作、源、目标、结果",
       "应用意外退出后重启时可提示「上次有未完成的文件操作，是否查看日志」"],
      "BC5 §10 + 用户要求：破坏性操作必须可逆或明确告知不可逆"),

    A("DIR-028", "文件夹比对", "在系统中定位与打开",
      "入口：右键菜单的「在资源管理器中显示」「用系统默认程序打开」。\n"
      "作用对象：本地文件/文件夹。\n"
      "边界：必须跨平台（Windows 资源管理器 / macOS Finder / Linux 文件管理器），"
      "并通过统一的平台服务实现而不是散落的 system() 调用。",
      ["「在资源管理器中显示」选中并聚焦到该条目（Windows 用 /select，macOS 用 open -R）",
       "「用系统默认程序打开」交给平台默认关联程序",
       "非本地数据源（远程/压缩包内/快照）时命令置灰并说明原因",
       "调用失败（无关联程序）给出可操作提示",
       "全部平台调用集中在一个可替换的服务组件中，单元测试可注入假实现"],
      "BC5 §5.2（文件操作与外部交互）+ TGIT（资源管理器右键集成）"),

    A("DIR-029", "文件夹比对", "双击打开与子视图路由",
      "入口：双击文件条目。\n"
      "作用对象：进入该文件对的比对子视图。\n"
      "边界：子视图的返回必须精确回到原来的滚动位置与选择，"
      "否则用户在大目录中会迷路。",
      ["双击文件对进入子视图（类型由文件格式定义决定），并提供面包屑返回",
       "返回文件夹视图时恢复滚动位置、选择集合与展开状态",
       "子视图标题包含相对路径，便于确认当前在目录树的哪个位置",
       "提供「打开到独立标签」的替代方式（中键或 Ctrl+双击）",
       "子视图内的修改（如复制块）能在返回后反映到文件夹状态（差异可能消失）"],
      "BC5 §9.2（双击打开）+ §2（会话内比较/打开）"),

    A("DIR-030", "文件夹比对", "外部工具与自定义命令",
      "入口：Tools 页 External Tools / Custom Commands 组；右键菜单。\n"
      "作用对象：把选中的文件/路径作为参数传给外部程序。\n"
      "边界：参数必须支持占位变量且做正确转义（含空格与中文路径），"
      "不允许拼接出无引号的命令行。",
      ["支持变量：左路径、右路径、选中项全路径、选中项文件名、选中项相对路径、当前目录",
       "多选时按参数展开策略（一次传一个 / 一次传全部 / 每个一行写入临时文件）",
       "路径含空格、中文、引号、换行时命令构造正确（有测试）",
       "外部程序退出码与输出被捕获并显示在输出面板",
       "自定义命令可新增/编辑/删除/排序，并支持导入导出"],
      "BC5 §31（外部调用）+ §34（自定义命令）+ Ailecium Services/Tools"),

    A("DIR-031", "文件夹比对", "文件名对齐规则（模糊配对）",
      "入口：会话设置 → 对齐/名称处理 Tab；Filters 页。\n"
      "作用对象：两侧文件名不完全相同但应视为同一逻辑文件的场景。\n"
      "边界：模糊配对是有风险的启发式，必须显式启用、结果可见、可随时关闭；"
      "绝不作为默认行为。",
      ["提供规则：忽略数字差异、忽略日期时间戳差异、忽略大小写、忽略扩展名大小写、自定义正则归一化",
       "被规则配对的两个条目在列中标注「模糊配对（含所用规则）」",
       "左侧一个条目匹配右侧多个条目时给出歧义警告，不静默取第一个",
       "关闭模糊配对后视图恢复为严格同名配对",
       "规则集合可保存为预设并导出"],
      "BC5 §8.3（规则化文件名对齐）+ §13.4（对齐覆盖）"),

    A("DIR-032", "文件夹比对", "名称不匹配仍比较内容（对齐覆盖）",
      "入口：会话设置 → 对齐覆盖 Tab。\n"
      "作用对象：强制把左侧某条目与右侧某条目配成一对。\n"
      "边界：覆盖只影响配对关系，不改变条目本身的文件系统身份。",
      ["可声明按掩码的对齐覆盖（如「*.h 与 *.hpp 视为同类」）",
       "可声明具体条目对的强制配对（左 pathA ↔ 右 pathB）",
       "覆盖规则按声明顺序匹配，命中即用；命中过程可日志查看",
       "覆盖生效的条目在视图中带标记，便于识别",
       "覆盖规则随会话保存并可导出为规则文件"],
      "BC5 §13.4（Alignment Override）"),

    A("DIR-033", "文件夹比对", "压缩包作为文件夹参与比对",
      "入口：Compare 页 Folder Options 组的「压缩包处理」下拉。\n"
      "作用对象：ZIP/TAR/GZ/7z 等归档文件。\n"
      "边界：本项只负责「以何种身份参与文件夹比对」，"
      "归档内部的读写由压缩包服务提供；不允许在文件夹比对里直接解析归档。",
      ["三档策略：作为普通文件（按整体比对）、打开会话时展开为文件夹、始终作为文件夹",
       "作为文件夹时归档在树中以文件夹图标呈现，且标注类型与压缩后大小",
       "归档内条目与普通目录条目在排序、过滤、状态判定上行为一致",
       "归档损坏或加密时标记为错误条目并说明原因",
       "归档内文件可双击进入子视图；优先流式/按需解压，超过内存预算时经明确设置允许使用受控临时缓存（ARC-002），不得整包无界展开"],
      "BC5 §25（压缩包内比对）"),

    A("DIR-034", "文件夹比对", "扫描进度、取消与增量刷新",
      "入口：状态栏与视图内的进度指示；刷新命令。\n"
      "作用对象：长耗时扫描过程。\n"
      "边界：刷新应尽量增量（复用未变化部分的 CRC 与状态），"
      "避免每次刷新都做全量重算。",
      ["状态栏显示扫描进度（已扫描条目数、已比对内容数、当前路径、耗时）",
       "提供取消按钮，取消后保留已得结果并标注不完整",
       "提供「刷新」命令，基于文件系统变化时间戳做增量复用；显示本次复用了多少缓存",
       "文件系统监视（可选）自动检测变化并在状态栏提示「检测到 N 项变化，点击刷新」",
       "刷新过程中不重置滚动位置与选择（尽量）",
       "提供「强制重新比较内容」跳过元数据缓存；普通刷新明确说明缓存依据，mtime/大小相同不构成新的字节相等证据"],
      "BC5 §8.3 + §36（性能）"),

    A("DIR-035", "文件夹比对", "文件夹比对的报表导出",
      "入口：Report 页的文件夹比较报表。\n"
      "作用对象：目录比对结果。\n"
      "边界：报表必须支持「只含差异」与「含全部」两种范围，"
       "默认只含差异以控制体积。",
      ["支持输出为 TXT / HTML / CSV / XML，含路径、状态、大小、时间、CRC、规则",
       "提供布局：摘要（只列差异条目）、并排（左右成列）、统计（按状态汇总计数）",
       "HTML 报表可逐层折叠，便于在大目录结果中定位",
       "报表生成显示进度可取消，大结果集分块写出避免内存峰值",
       "报表头部记录比较准则、忽略规则、过滤条件，保证结论可复现"],
      "BC5 §16.4（文件夹比较报表）+ §16.2（布局）"),

    A("DIR-036", "文件夹比对", "UNC、系统挂载网络位置与长路径支持",
      "入口：数据源为 UNC 路径、映射网络驱动器、WebDAV 或超长路径。\n"
      "作用对象：非本地常规路径。\n"
      "边界：本期仅覆盖操作系统已挂载的网络位置与 UNC；原生 WebDAV/FTP/SFTP/云连接依 OPT-009 延后。错误需区分网络不可达、认证/权限、路径过长，后台 I/O 可取消。",
      ["支持 UNC 路径（\\\\server\\share）与映射盘符",
       "Windows 上超过 MAX_PATH 的路径通过扩展长度前缀处理",
       "网络位置不可达时给出「网络不可达」而不是「无权限」，并区分认证失败",
       "网络位置扫描的超时与重试策略可配置，避免长时间挂起",
       "网络性能差时 UI 不冻结（全部 I/O 在后台线程）"],
      "BC5 §36（文件系统细节）+ §27（远程与云服务）"),

    A("DIR-037", "文件夹比对", "符号链接、硬链接与特殊文件处理",
      "入口：Compare 页 Folder Options 组的「跟随符号链接」；会话设置相关项。\n"
      "作用对象：符号链接、硬链接、设备文件、FIFO。\n"
      "边界：默认不跟随符号链接（避免循环与意外越界），"
      "跟随必须是显式选择且要有循环检测。",
      ["提供「跟随符号链接」开关，默认关闭",
       "链接本身作为条目显示，并标注链接目标与「链接」类型",
       "开启跟随后检测循环并终止，循环路径记录为错误条目",
       "硬链接（同一 inode 多路径）被识别并可选择「视为同一文件」",
       "设备文件/FIFO 不参与内容比对，只按类型与元数据比对"],
      "BC5 §36（符号链接与权限）+ §5.2 新增（Unix 文件类型）"),

    A("DIR-038", "文件夹比对", "文件夹比对的自动化测试与语料",
      "入口：测试工程的文件夹比对套件。\n"
      "作用对象：扫描、配对、状态判定、文件操作。\n"
      "边界：测试必须在临时目录内自建语料，不依赖仓库内的固定目录，"
       "且不污染用户目录。",
      ["套件在临时目录构造语料：仅左/仅右/同内容/不同内容/时间差/大小写差/子目录/空目录/深层嵌套",
       "断言主状态及时间/完整性维度正确，覆盖内容覆盖快速测试、规则比较不被大小短路及未知不能转为相同的组合",
       "文件操作测试（复制/移动/删除/重命名/镜像）在临时目录执行并断言结果，删除走临时回收区",
       "失败路径测试：只读目录、无权限目录、目标空间不足（用模拟层）",
       "套件在 macOS 与 Windows 上均可运行，且在 60s 内完成"],
      "Ailecium Code/Tests + docs/development/test-runner.md 约定"),

    # ------------------------------------------------------------------
    # SYNC —— 文件夹同步
    # ------------------------------------------------------------------
    A("SYNC-001", "文件夹同步", "双向同步会话与三栏预览布局",
      "入口：新建文件夹同步会话；命令行 `/sync`。\n"
      "作用对象：左目录、操作清单、右目录三栏。\n"
      "边界：同步会话的核心是「先预览后执行」；"
      "预览未生成时不提供直接执行入口。",
      ["三栏布局：左目录树 / 待执行操作清单 / 右目录树",
       "操作清单按方向分组（左→右、右→左、删除、跳过）并显示每项动作与原因",
       "未生成预览前「执行」按钮禁用，并说明「请先生成预览」",
       "预览可重新生成，生成过程可取消",
       "左右目录可在会话内直接切换并自动重新预览"],
      "BC5 §11（文件夹同步）+ §1 SESS-011"),

    A("SYNC-002", "文件夹同步", "同步方向与同步规则",
      "入口：同步会话设置 → 同步 Tab。\n"
      "作用对象：单向（左→右 / 右→左）与双向的复制决策。\n"
      "边界：方向与规则共同决定每项的动作；"
      "规则不能与方向矛盾，矛盾时必须报错而不是猜。",
      ["方向：左→右、右→左、双向；单向可选择更新或镜像，双向默认按 SYNC-006 基线判定，取新属于显式冲突策略",
       "规则：复制源侧独有条目、按显式选择复制较新/较旧、镜像（源范围内使目标一致）；不存在「两侧都不存在者」可供复制",
       "双向冲突统一复用 SYNC-006：无基线时两侧存在且内容不同即冲突，与时间戳是否相同无关",
       "规则组合非法时（如「镜像」+「保留目标多余文件」）在设置界面就地报错",
       "方向与规则写入会话并可被命令行覆盖"],
      "BC5 §11（Folder Sync 规则）+ §12"),

    A("SYNC-003", "文件夹同步", "同步预览与操作清单交互",
      "入口：同步会话主界面的「生成预览」。\n"
      "作用对象：将执行的每项文件系统操作。\n"
      "边界：用户可以在预览中否决单条操作；"
      "被否决的项在执行时被跳过且日志中记录。",
      ["清单每行显示：动作（复制/删除/新建目录/覆盖）、源、目标、大小、原因（规则命中说明）",
       "可勾选/取消单条或多条操作，支持按类型批量勾选",
       "可对单条操作「反转方向」",
       "清单可导出为文本/CSV，便于在真正执行前审阅",
       "存在疑项（如大小相同但时间不同、目标为只读）时以警示图标列出"],
      "BC5 §11（同步预览）"),

    A("SYNC-004", "文件夹同步", "同步执行、进度与失败处理",
      "入口：预览界面的「执行」。\n"
      "作用对象：勾选的操作清单。\n"
      "边界：批量写操作必须先生成预演与影响摘要；GUI 确认或脚本显式执行授权后运行。覆盖默认备份、删除默认回收站，并如实报告每项恢复能力。",
      ["执行前确认框显示：将复制 N 个文件（共 X 大小）、将删除 M 个条目、将新建 K 个目录",
       "执行过程显示整体进度与当前文件进度，可取消",
       "取消后已完成部分保留，并生成「已执行/未执行」清单",
       "单文件失败不中断整体，失败项汇总为清单并可「重试失败项」",
       "执行完成后自动重新预览以反映最新状态",
       "执行前重验操作清单的源/目标指纹及扫描完整性；预演后发生修改、路径替换或扫描不完整时该项失效，重新预演，不按旧清单覆盖或删除"],
      "BC5 §11（同步执行）+ §10（复制）"),

    A("SYNC-005", "文件夹同步", "同步删除策略与安全阈值",
      "入口：同步会话设置 → 删除 Tab。\n"
      "作用对象：目标侧多余条目的处理。\n"
      "边界：删除是同步最危险的部分，默认必须走回收站，"
      "且必须有数量/体积阈值保护。",
      ["删除策略三档：不删除、删除到回收站（默认）、直接永久删除",
       "提供阈值保护：单次同步删除超过 N 个条目或超过 X MB 时额外确认（阈值可配置）",
       "阈值触发时明确列出将被删除的条目数与总大小，并要求输入确认语或二次勾选",
       "删除失败（被占用/无权限）的条目保留并记录，不静默跳过",
       "全部删除记入日志；回收站删除提供还原入口，显式永久删除明确标记不可恢复"],
      "BC5 §11 + 用户要求：破坏性操作必须显式确认与可逆"),

    A("SYNC-006", "文件夹同步", "同步冲突处理",
      "入口：双向同步时出现两侧均改动；同步设置 → 冲突 Tab。\n"
      "作用对象：冲突条目的决策。\n"
      "边界：冲突默认不自动解决；"
      "任何自动策略都必须可选且默认关闭。",
      ["冲突判定：两侧相对上次同步均有变化（需要快照作为基线，见 SNAP 模块）",
       "无基线时（首次同步）把「两侧存在且内容不同」视为冲突",
       "冲突策略：跳过（默认）/ 取较新 / 取较大 / 取指定侧 / 保留两份（重命名副本）",
       "冲突在预览清单中以醒目样式单独分组",
       "提供冲突的逐项手工决策界面，可打开文件比对子视图对照后决定"],
      "BC5 §11（同步冲突）+ §12"),

    A("SYNC-007", "文件夹同步", "同步后的校验",
      "入口：同步设置 → 校验 Tab。\n"
      "作用对象：复制结果与源的字节一致性。\n"
      "边界：校验会显著增加耗时，默认关闭但强烈建议在关键场景开启；"
      "校验失败必须明确报告而不是忽略。",
      ["可选校验方式：无校验 / 大小校验 / CRC32 / 逐字节",
       "校验失败的文件在清单中标红并给出失败原因（大小不符/内容不符）",
       "校验失败时不删除源，并提供「重试复制」与「保留目标当前状态」两条出路",
       "校验结果汇总为报告并记录在同步日志",
       "校验在同步执行阶段就地完成，不额外整体重扫一遍"],
      "BC5 §11 + §8.2（内容比对）"),

    A("SYNC-008", "文件夹同步", "同步日志与报告",
      "入口：同步设置中的日志项；同步完成后的报告入口。\n"
      "作用对象：操作留痕。\n"
      "边界：日志必须足以在事后审计「谁在什么时候把哪个文件从哪复制到哪」。",
      ["日志格式：时间、动作、源路径、目标路径、字节数、结果、失败原因",
       "可选输出到文件（追加式）与控制台；日志文件路径可配置并自动轮转",
       "同步报告包含摘要（复制/删除/新建/跳过/失败计数）与明细，可导出 TXT/HTML/CSV",
       "报告默认存于用户日志目录；若用户选择目标目录，报告路径须显式排除出本次和后续同步源范围，避免自生成差异",
       "日志中包含本次同步的规则与选项快照，保证可复现"],
      "BC5 §11 + §16（报表）"),

    A("SYNC-009", "文件夹同步", "同步会话预设与批量执行",
      "入口：Session 页 Save/Defaults；命令行。\n"
      "作用对象：可重复使用的同步配置。\n"
      "边界：预设保存的是规则与选项，不保存具体目录内容；"
      "预设可被命令行引用以便脚本化。",
      ["同步会话可命名保存，包含方向、规则、过滤、校验、日志选项",
       "提供「从预设新建同步会话」的快捷入口",
       "命令行可指定预设并配 `/sync` 执行（支持静默模式与返回码）",
       "预设可导出/导入为文件，便于在多台机器间共享",
       "预设变更不影响已保存的历史同步日志"],
      "BC5 §11 + §29（命令行）+ §1 SESS-011"),

    A("SYNC-010", "文件夹同步", "同步的过滤与范围限定",
      "入口：同步会话设置 → 过滤 Tab；Filter 页。\n"
      "作用对象：哪些条目参与同步。\n"
      "边界：过滤必须同时作用于「扫描」与「删除判定」——"
      "否则被过滤掉的目标文件会被误判为「多余」而删除。",
      ["支持包含/排除掩码、名称正则、大小与时间条件过滤",
       "被过滤的条目既不被复制也不被判定为多余（不出现在删除清单中），这一语义有测试",
       "扫描范围排除项始终受到删除保护；「包含显示隐藏项」仅影响显示过滤，不得关闭范围排除保护；扩大同步范围须更改过滤并重新预演",
       "过滤生效时预览清单与状态栏都标注「已按过滤条件限定范围」",
       "过滤条件写入会话预设"],
      "BC5 §11 + §13（过滤与掩码）"),

    A("SYNC-011", "文件夹同步", "同步的基线快照",
      "入口：同步会话设置 → 快照 Tab。\n"
      "作用对象：为双向同步提供「上次同步状态」基线。\n"
      "边界：无基线时双向前向同步会退化为保守模式；"
      "基线必须与目录绑定并带完整性校验。",
      ["可在每次成功同步后自动生成/更新该目录对的基线快照",
       "快照记录每个条目的相对路径、大小、修改时间与可选 CRC",
       "快照用于判定「哪一侧发生了变化」，从而把双向同步的冲突降到最少",
       "快照损坏或缺失时自动降级为无基线模式并明确提示",
       "快照可在同步会话中查看、导出、删除与手动指定",
       "只有确认成功的同步范围才可更新基线；失败、取消、未知或部分比较不能写成已同步；缓存须绑定两侧身份、规则和范围，变更后需重建"],
      "BC5 §11 + §26（快照）"),

    A("SYNC-012", "文件夹同步", "同步会话的自动化测试",
      "入口：测试工程的同步套件。\n"
      "作用对象：预览生成、规则决策、执行、删除保护。\n"
      "边界：测试在临时目录内完成，不触碰真实用户数据；"
       "删除相关用例必须验证默认走临时回收区。",
      ["语料覆盖：镜像、更新（仅复制较新）、双向取新、双向冲突、目标多余文件、过滤保护",
       "断言预览清单的每一项动作与原因字符串稳定（快照比对）",
       "执行用例断言文件系统最终状态（目录树 + 每个文件内容摘要）",
       "删除保护：超阈值时不带确认不得执行删除",
       "失败注入：目标只读、文件被占用时断言不中断整体并生成失败清单"],
      "Ailecium Code/Tests 约定"),

    # ------------------------------------------------------------------
    # FMG —— 文件夹合并
    # ------------------------------------------------------------------
    A("FMG-001", "文件夹合并", "文件夹合并会话与三路布局",
      "入口：新建文件夹合并会话；命令行 `/fv=\"Folder Merge\"`。\n"
      "作用对象：左侧版本、右侧版本、祖先、合并输出四个目录。\n"
      "边界：合并输出是一个真实目录；"
      "在任何写操作发生前必须保证输出目录状态明确（空目录或已有目录的选择）。",
      ["四栏布局：左 / 祖先 / 右 / 合并输出，均以目录树呈现",
       "每栏可展开到文件级，文件级决策时进入文本/二进制比对子视图",
       "合并输出栏显示当前计划结果（哪些条目将被写入、来自哪侧）",
       "关闭会话时若存在未应用的决策或被修改的输出目录，给出提示",
       "三侧目录缺失祖先时降级为两路合并并明确提示"],
      "BC5 §12（文件夹合并）+ §1 SESS-010"),

    A("FMG-002", "文件夹合并", "逐条目合并决策",
      "入口：合并输出栏内右键菜单 / Merge 页命令。\n"
      "作用对象：单个文件或文件夹的归属决策。\n"
      "边界：决策只记录在会话计划中，真正落盘由「应用」显式触发；"
      "决策与落盘分离是安全前提。",
      ["对条目可选：取左侧 / 取右侧 / 取祖先 / 重命名保留两份 / 跳过（不输出）",
       "决策立即在合并输出栏反映（计划态），文件系统不立即变化",
       "每条决策有可解释状态：哪些侧有改动、相对祖先的差异摘要",
       "决策可撤销，撤销后回到自动推导的计划",
       "提供「全部取左侧」「全部取右侧」「只应用无冲突项」的批量入口并二次确认"],
      "BC5 §12 + TGIT §C.3 Page 3"),

    A("FMG-003", "文件夹合并", "目录级决策传播",
      "入口：在文件夹节点上做决策。\n"
      "作用对象：整个子树。\n"
      "边界：目录级决策与子项已有决策的冲突必须显式处理，"
      "不能静默覆盖用户已做的逐项决策。",
      ["在文件夹上选「取左侧」时，子树全部采用左侧（含右侧独有的条目处理按策略）",
       "若子树内已有不同的逐项决策，弹窗列出冲突项并让用户选择「保留我的逐项决策」或「覆盖」",
       "提供「只对未决策项应用目录级决策」的安全选项",
       "目录级决策在树中以父节点标记形式可见（如显示「左」角标）",
       "展开子树时逐项决策仍单独可见"],
      "BC5 §12（文件夹合并的目录处理）"),

    A("FMG-004", "文件夹合并", "冲突文件夹与冲突标识",
      "入口：合并会话扫描后自动标记。\n"
      "作用对象：两侧相对祖先均有改动且不一致的条目。\n"
      "边界：冲突判定只依据「相对祖先两侧都有改动且不相同」，"
      "不使用时间戳新旧之类的启发式；时间上无法判定时标记为冲突。",
      ["冲突条目在四栏与合并栏中有统一醒目的标识",
       "文件夹级冲突由子项冲突推导得出，父节点显示冲突计数徽标",
       "支持「只显示冲突」过滤器，折叠无冲突子树",
       "冲突清单面板列出全部冲突（相对路径、类型、两侧摘要、决策状态）",
       "冲突数量与已决策数量在状态栏实时可见"],
      "BC5 §12 + §11（冲突）"),

    A("FMG-005", "文件夹合并", "合并输出的写回策略与应用",
      "入口：Merge 页的「应用合并结果」。\n"
      "作用对象：合并输出目录与可选的目标目录。\n"
      "边界：应用是批量文件系统操作，必须有完整预演、确认、进度、可取消与失败清单；"
      "默认不覆盖非空输出目录中的未知条目。",
      ["应用前生成完整的操作清单（新增/覆盖/删除/跳过）并要求确认",
       "支持三种输出模式：写入全新空目录、覆盖指定目录、原地合并到某一侧（危险，需额外确认）",
       "默认保留输出目录中未被计划涉及的条目（不视为多余而删除）",
       "应用过程显示进度、可取消，取消后保留已完成部分并生成清单",
       "应用完成后生成合并报告（决策汇总 + 实际操作清单 + 校验结果）"],
      "BC5 §12（文件夹合并的写回）+ §10"),

    A("FMG-006", "文件夹合并", "文件夹合并预览与模拟执行",
      "入口：Merge 页的「预览合并结果」。\n"
      "作用对象：应用后的目录树状态。\n"
      "边界：预览必须与实际应用结果一致；"
      "实现上应复用同一条计划生成路径，避免预览与应用两套逻辑。",
      ["预览以目录树形式显示应用后的目标目录结构（含每个条目的来源标记）",
       "预览与应用共享同一份操作清单对象（同一代码路径，有测试保证一致）",
       "预览中标出「将被删除」的条目且默认不删除被排除项",
       "预览可导出为文本清单",
       "若输出目录在预览后被外部修改，应用时检测并提示重新预览"],
      "BC5 §12 + §11（同步预览）"),

    A("FMG-007", "文件夹合并", "文件夹合并的批量决策与筛选联动",
      "入口：合并会话的工具条批量操作区。\n"
      "作用对象：按过滤条件选出的条目集合。\n"
      "边界：批量决策的作用范围必须显式显示（当前可见 N 项 vs 全部 M 项），"
      "禁止在用户不知范围的情况下批量决策。",
      ["支持「对当前选中项」「对当前过滤结果」「对全部冲突项」三种范围",
       "范围选择器显示具体数量，并在执行前确认框中重复一次",
       "批量决策支持排除已人工决策项（默认开启）",
       "批量决策可撤销（作为一次原子操作撤销）",
       "批量决策后刷新统计与冲突计数"],
      "BC5 §12 + §9.4（选择与批量操作）"),

    A("FMG-008", "文件夹合并", "文件夹合并报表与审计",
      "入口：Report 页的文件夹合并报表。\n"
      "作用对象：合并决策记录。\n"
      "边界：报表必须能作为「这次合并做了什么」的凭证，"
       "包含每一步的来源与时间。",
      ["报表包含：三侧路径、祖先路径、输出路径、每条目的决策与来源、冲突列表与解决方式",
       "支持 TXT / HTML / CSV / XML 输出，HTML 可折叠",
       "报表头部记录会话设置（比较准则、忽略规则、时间）",
       "报表可导出到输出目录并在文件名中带时间戳",
       "报表生成不修改会话状态，可重复生成"],
      "BC5 §12 + §16（报表）"),

    A("FMG-009", "文件夹合并", "文件夹合并的撤销与应用前检查",
      "入口：应用前的自动检查；应用后的撤销入口。\n"
      "作用对象：即将执行的写操作。\n"
      "边界：覆盖已有输出前默认建立可验证备份，备份失败停止该项；恢复能力不足或用户明确关闭备份时逐项说明不可撤销并二次确认。写入全新目录可整体撤销。",
      ["应用前检查：目标目录可写、磁盘空间充足、路径长度合法、无文件被独占锁定",
       "检查失败时列出全部不可执行原因，不逐条弹窗打扰",
       "写入新目录的场景下提供「撤销本次合并」把输出目录移到回收站",
       "原地覆盖前默认生成并校验备份；确认框列明备份位置与恢复方式，关闭备份必须显式确认不可撤销风险",
       "应用与撤销操作均记录在操作日志"],
      "BC5 §12 + 用户要求：破坏性操作必须先备份、显式确认"),

    A("FMG-010", "文件夹合并", "文件夹合并的自动化测试",
      "入口：测试工程的文件夹合并套件。\n"
      "作用对象：计划生成、预览一致性、应用结果、目录级传播。\n"
      "边界：测试在临时目录内自建三侧目录与祖先，"
       "断言最终目录状态与计划完全一致。",
      ["语料覆盖：仅左改、仅右改、两侧同改、两侧异改（冲突）、仅左有、仅右有、目录整体替换",
       "断言目录级决策传播与逐项决策冲突时的交互行为（保留/覆盖两种选择）",
       "断言「预览与实际应用一致」：先预览再应用，比对两者操作清单逐字段相同",
       "断言原地覆盖场景不做隐含备份以外的删除",
       "套件在 macOS 与 Windows 上均可运行"],
      "Ailecium Code/Tests 约定"),
]
