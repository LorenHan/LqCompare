# -*- coding: utf-8 -*-
"""过滤规则（FILT）、文件格式（FMT）、报表导出（RPT）、补丁（PAT）、快照（SNAP）规格。"""

from . import A

ACTIONS = [
    # ------------------------------------------------------------------
    # FILT —— 过滤与掩码
    # ------------------------------------------------------------------
    A("FILT-001", "过滤规则", "文件掩码语法与解析器",
      "入口：Filters 页的包含/排除掩码；会话设置 → 名称过滤。\n"
      "作用对象：文件名与路径的匹配判定。\n"
      "边界：掩码语法必须自成一体、可测试、有文档；"
      "不允许各处实现各自的通配逻辑。",
      ["支持 `*`（任意字符）、`?`（单字符）、`[...]`（字符集）、`**`（跨目录）等语义",
       "支持排除掩码（前导 `-`）与包含掩码同时声明，排除优先",
       "掩码匹配在 Windows 上默认大小写不敏感、Unix 上默认大小写敏感，且可显式覆盖",
       "提供掩码语法速查与实时预览（输入掩码后显示「匹配 N 项 / 共 M 项」）",
       "解析器为纯函数并有完整单元测试（含恶意与畸形输入）"],
      "BC5 §13.1（文件掩码语法）"),

    A("FILT-002", "过滤规则", "名称过滤器（正则与实体名）",
      "入口：Filters 页名称过滤输入框。\n"
      "作用对象：按条目名称的高级匹配。\n"
      "边界：正则表达式必须做灾难性回溯防护（超时保护），"
      "否则一个恶意正则会冻结整个扫描。",
      ["支持精确名、通配符、以及正则三种匹配模式并可切换",
       "正则匹配有超时保护（默认 200ms/条），超时记录为错误条目并继续",
       "支持「包含任一」「不包含任何」「全部满足」三种组合语义，且界面明确显示当前语义",
       "过滤表达式的编辑有实时校验，非法正则就地报错且不生效",
       "表达式可命名保存为预设并导出"],
      "BC5 §13.2（名称过滤器）"),

    A("FILT-003", "过滤规则", "属性过滤（大小 / 时间 / 属性位）",
      "入口：Filters 页属性过滤组。\n"
      "作用对象：按条目元数据过滤。\n"
      "边界：属性过滤必须与内容比对解耦——"
      "被属性过滤掉的条目不应被读取内容（性能关键）。",
      ["支持按大小范围过滤（含最小值/最大值，支持 KB/MB/GB 单位）",
       "支持按修改时间范围过滤（相对时间如「最近 7 天」与绝对区间）",
       "支持按属性位过滤（只读/隐藏/系统/归档）与按所有者/组过滤（Unix）",
       "多个属性条件之间为逻辑与，并与名称过滤构成整体的与关系",
       "过滤在扫描阶段早期生效，被过滤条目不被读取内容（有性能断言）"],
      "BC5 §13.3（其它过滤器）"),

    A("FILT-004", "过滤规则", "内容过滤器（行过滤与关键字节）",
      "入口：Filters 页内容过滤组；文件格式定义中的行过滤器。\n"
      "作用对象：文件内容层面的过滤。\n"
      "边界：内容过滤需要读取文件，性能代价高，"
      "必须显式启用且给出性能提示。",
      ["支持「行过滤器」：排除匹配指定模式的行后再比对（如日志时间戳行）",
       "支持「关键字节过滤」：仅当二进制文件包含指定字节序列时才纳入比较",
       "内容过滤与忽略规则的作用顺序明确（先过滤行再应用忽略规则）且有测试",
       "启用内容过滤时状态栏提示「内容过滤已启用，比较速度会降低」",
       "内容过滤条件可保存为文件格式定义的一部分"],
      "BC5 §13.3 + §14.2（转换）"),

    A("FILT-005", "过滤规则", "过滤器的层级与作用域",
      "入口：会话设置 → 过滤 Tab 与 Filters 页。\n"
      "作用对象：多个过滤来源的叠加规则。\n"
      "边界：层与层之间的关系必须明确（并集还是交集），"
      "并且界面要能显示「最终生效的过滤表达式」。",
      ["过滤器层级：文件格式定义内建过滤 → 会话设置过滤 → 视图临时过滤，三层叠加",
       "每层可单独启用/禁用，并显示该层是否当前生效",
       "提供「查看最终生效过滤」面板，展示三层合并后的表达式与匹配计数",
       "视图临时过滤不写入会话，关闭标签即丢弃",
       "层级语义有单元测试（每层的包含/排除关系）"],
      "BC5 §13（过滤与文件掩码）+ §1（Session Settings）"),

    A("FILT-006", "过滤规则", "过滤结果的可见性与批量操作安全",
      "入口：任何批量操作（复制/删除/同步）执行时。\n"
      "作用对象：被过滤条目在批量操作中的地位。\n"
      "边界：核心安全规则——被显示过滤器隐藏的条目，"
      "在批量操作中不得被静默包含或静默排除；必须显式询问或明确标注。",
      ["批量操作必须明确「作用范围为当前可见项」并在确认框中写明数量",
       "存在被隐藏条目时确认框追加提示「另有 N 个条目因过滤而不可见，将不被处理」",
       "提供「包含被隐藏项」的显式勾选（默认不勾选）",
       "该安全规则有测试：构造隐藏条目并断言两个选项下的行为差异",
       "状态栏常显「过滤生效：可见 N / 共 M」以降低误操作概率"],
      "Ailecium docs/PRD-actions.md（选择/过滤与批量删除的安全边界）"),

    A("FILT-007", "过滤规则", "掩码与过滤器预设库",
      "入口：Filters 页掩码预设画廊；选项中的预设管理。\n"
      "作用对象：常用过滤组合的复用。\n"
      "边界：预设是数据而不是代码，"
      "用户新增预设不需要改程序。",
      ["内置预设：源码（排除编译产物）、文档、图片、日志、版本控制元数据（.git/.svn）等",
       "用户可新增/编辑/删除预设，可命名与备注",
       "预设可导出/导入为文件，便于团队共享",
       "预设应用后可在过滤输入框中看到实际写入的表达式（不是黑盒）",
       "预设库存储在用户配置目录，升级不覆盖用户新增项"],
      "BC5 §13 + §37（设置存储）"),

    A("FILT-008", "过滤规则", "临时过滤与快速排除",
      "入口：文件夹视图右键「隐藏此类文件」/「只显示此类文件」；Filter 页快捷输入。\n"
      "作用对象：不改变会话设置的临时视图过滤。\n"
      "边界：临时过滤必须与「视图状态快照」一起被记住，"
       "但不能写入会话文件污染默认值。",
      ["右键可对选中条目「隐藏该扩展名」「只显示该扩展名」「隐藏该名称」",
       "临时过滤条件在界面顶部以可移除的标签形式列出",
       "提供「清除全部临时过滤」命令",
       "临时过滤在切换子视图与返回后保持；关闭标签后丢弃",
       "临时过滤不写入会话文件（有测试：保存会话后重开，临时过滤不出现）"],
      "BC5 §9.3（显示过滤器）"),

    A("FILT-009", "过滤规则", "过滤的性能与索引",
      "入口：扫描阶段的过滤应用。\n"
      "作用对象：过滤对扫描速度的影响。\n"
      "边界：过滤必须尽早生效——"
      "在目录枚举阶段就排除子树，而不是枚举完再过滤。",
      ["被排除的目录子树在枚举阶段直接跳过（不做目录内遍历）",
       "掩码匹配结果按目录做记忆化，避免同目录多次重复匹配",
       "过滤开启与关闭的扫描耗时差异在测试中被度量（预期过滤更快）",
       "大目录（>10 万条目）下过滤匹配总耗时有上界断言",
       "过滤的正则超时保护不会显著拖慢整体（超时项有计数）"],
      "BC5 §13 + §36（性能）"),

    A("FILT-010", "过滤规则", "子目录级别的过滤与展开排除",
      "入口：会话设置 → 文件夹处理中的子目录过滤。\n"
      "作用对象：整个子目录被排除或强制包含。\n"
      "边界：子目录级的排除会掩盖内部差异，"
      "必须在状态栏与报表中明确标注「已排除 N 个子目录」。",
      ["支持按目录名/路径掩码排除整个子树",
       "白名单仅在本层没有显式排除时补充包含范围，不越过 FILT-001 排除优先与 FILT-005 跨层交集；临时包含须显式停用相应排除规则并重新扫描",
       "被排除的目录在树中不可见，但在状态栏计数中明确列出数量",
       "报表头部记录被排除的目录清单，保证结论可解释",
       "提供「临时包含被排除目录」的快捷方式便于抽查"],
      "BC5 §13.3 + §8.3"),

    A("FILT-011", "过滤规则", "掩码语法与过滤行为的文档与帮助",
      "入口：Help 页；过滤输入框旁的问号。\n"
      "作用对象：用户理解过滤行为。\n"
      "边界：文档必须与实现同源（关键用例来自测试语料），"
      "避免文档与行为不一致。",
      ["提供掩码语法速查页，含表格化的通配符说明与示例",
       "过滤面板内可一键打开速查（不跳转外部浏览器）",
       "文档中的示例掩码与测试语料中的用例一致（用同一个数据源生成）",
       "提供「我为什么看不到某个文件」的诊断：输入文件名，报告被哪一层过滤命中",
       "帮助内容可离线查看"],
      "BC5 §13.1 + §38（帮助）"),

    A("FILT-012", "过滤规则", "过滤规则的测试语料",
      "入口：测试工程的过滤套件。\n"
      "作用对象：掩码、正则、属性过滤、层级叠加。\n"
      "边界：语料覆盖真实项目结构与恶意输入。",
      ["掩码语料：`*`、`?`、`[a-z]`、`**`、`*.tar.gz`、前导 `-`、大小写差异、含 Unicode 名称",
       "正则语料：合法/non-合法、超长输入、灾难性回溯样本（断言超时保护生效）",
       "属性过滤语料：大小边界值、时间边界值、属性位组合",
       "层级叠加语料：三层包含/排除的组合结果",
       "断言过滤在扫描早期生效（构造大目录断言语义等价但耗时更短）"],
      "Ailecium Code/Tests 约定"),

    # ------------------------------------------------------------------
    # FMT —— 文件格式定义
    # ------------------------------------------------------------------
    A("FMT-001", "文件格式", "文件格式定义模型与存储",
      "入口：Tools 页「文件格式管理」。\n"
      "作用对象：每种文件格式的比较规则集合。\n"
      "边界：格式定义必须是可序列化的数据（不是编译期常量），"
      "并且必须支持用户自定义与导入导出。",
      ["格式定义字段：名称、掩码列表、默认视图类型、编码与行尾策略、语法定义、转换规则、行过滤器、重要性规则、列/字段定义、杂项选项",
       "定义以文本格式存储（便于 diff 与版本管理），支持继承/覆盖内置定义",
       "用户定义与内置定义分离存放，升级不覆盖用户修改",
       "定义有唯一稳定的 ID，重命名不影响关联",
       "定义文件损坏时跳过该定义并报错，不影响其它定义加载"],
      "BC5 §14.1（格式管理器）+ §37（设置存储）"),

    A("FMT-002", "文件格式", "格式管理界面",
      "入口：Tools 页 → 文件格式。\n"
      "作用对象：格式定义的增删改查。\n"
      "边界：格式之间的优先级顺序决定掩码命中结果，"
      "必须显式可调并且界面可见。",
      ["左侧列出全部格式（内置置灰不可删，用户定义可编辑），可拖拽调整优先级顺序",
       "右侧为选中格式的详细设置（分 Tab：常规、转换、语法、杂项）",
       "支持新建、复制、删除、导入、导出单个或全部格式定义",
       "支持「哪些文件使用此格式」的反向查询（输入文件名验证命中）",
       "修改内置格式时提示会创建用户覆盖副本，不改动原定义"],
      "BC5 §14.1 + §13.4（格式关联覆盖）"),

    A("FMT-003", "文件格式", "文本格式：语法定义",
      "入口：文件格式 → 语法 Tab。\n"
      "作用对象：词法高亮与「忽略注释」所需的语法信息。\n"
      "边界：语法定义同时服务于高亮与注释忽略，"
      "两处必须共用同一份定义避免行为不一致。",
      ["可定义注释：行注释前缀、块注释起止、嵌套块注释开关",
       "可定义字符串定界符与转义规则",
       "可定义关键字/类型/数字/预处理指令的正则列表",
       "可定义「注释内不识别字符串」等交互规则",
       "语法定义变化后高亮与忽略注释行为同时更新（同一份定义驱动）"],
      "BC5 §14.3（文本格式：语法）"),

    A("FMT-004", "文件格式", "文本格式：转换（比较前的文本预处理）",
      "入口：文件格式 → 转换 Tab。\n"
      "作用对象：比对前对文件内容的规范化。\n"
      "边界：区分仅比较规范化与真实格式转换。规范化保留原文编辑模型；不可逆转换视图默认只读，只有配置并验证反向保存转换才允许写回，禁止用原内容假装保存了编辑。",
      ["比较规范化支持空白、Tab、行尾、BOM、编码等规则，保留原始字节和原文定位，不把规范化直接当编辑内容",
       "自定义正则替换可多条按序应用；真实外部转换声明加载与可选保存程序，并复用进程服务处理退出码、取消、超时和临时文件",
       "转换链可调序，界面展示最终生效链和是否可逆",
       "显示转换后内容时明确标注；没有可靠反向转换则只读，可另存转换后的新文件",
       "允许编辑的转换须通过加载→编辑→保存→重新加载往返验证；保存前备份且原子替换，不能丢弃用户编辑却报告保存成功"],
      "BC5 §14.2；官方 https://www.scootersoftware.com/v5help/formattextconversion.html"),

    A("FMT-005", "文件格式", "数据格式与表格列定义",
      "入口：文件格式 → 数据 Tab。\n"
      "作用对象：表格类文件的列语义。\n"
      "边界：列定义按「列名或列序号」匹配，"
      "两者语义差异必须明确（列名更稳但改名即失效）。",
      ["可定义分隔符、引号、转义、编码、表头行、注释行前缀",
       "可定义命名列及其类型（文本/数值/日期/布尔）与比较语义",
       "可定义键列（Key Columns）与忽略列",
       "列匹配可按列名或列序号，界面明确显示当前匹配方式",
       "列定义变更后表格比对视图立即按新定义重算"],
      "BC5 §14.5（数据格式与图片格式）+ §19"),

    A("FMT-006", "文件格式", "图片格式定义",
      "入口：文件格式 → 图片 Tab。\n"
      "作用对象：图片比对的默认容差与显示选项。\n"
      "边界：图片格式定义只提供默认值，"
      "用户在会话中的修改优先。",
      ["可定义默认容差、默认差异显示模式、是否比对元数据",
       "可按扩展名归组（如所有 .png 用同一套默认）",
       "可定义「按文件名模式使用不同容差」（如截图对比用严格、照片对比用宽松）",
       "定义生效后新打开的图片会话采用该默认值",
       "已有会话不因定义变更而改变设置"],
      "BC5 §14.5 + §20"),

    A("FMT-007", "文件格式", "归档格式定义的识别与处理",
      "入口：文件格式 → 归档 Tab。\n"
      "作用对象：哪些文件被识别为归档，以及如何展开。\n"
      "边界：归档识别必须按内容嗅探与扩展名双重判定，"
      "因为扩展名常被随意更名。",
      ["可定义归档掩码与内容嗅探签名（magic bytes）",
       "定义「始终展开 / 仅在打开会话时展开 / 作为普通文件」的默认策略",
       "支持按归档类型指定解压器与参数",
       "归档格式定义变化后文件夹比对中的处理方式随之变化",
       "嗅探与扩展名冲突时（如 .zip 实为文本文件）给出提示并说明判定依据"],
      "BC5 §25 + §14.1"),

    A("FMT-008", "文件格式", "格式关联覆盖（Association Override）",
      "入口：Filters 页或会话设置中的格式关联覆盖。\n"
      "作用对象：为特定文件对强制指定文件格式。\n"
      "边界：覆盖是会话级设置，不改变全局格式定义的优先级。",
      ["可声明「某通配名称的文件对使用某个文件格式」，如 `*.log 用 MyLog 格式`",
       "覆盖按声明顺序匹配，命中即用，并在状态栏显示「当前使用 XXX 格式」",
       "覆盖可在会话中临时添加/移除，不影响全局定义",
       "覆盖列表可保存进会话并导出为规则文件",
       "命中覆盖时在格式查询诊断中明确显示来源为覆盖而非定义"],
      "BC5 §13.4（格式关联覆盖）+ §14.1"),

    A("FMT-009", "文件格式", "未知扩展名的兜底策略",
      "入口：选项 → 文件格式默认行为。\n"
      "作用对象：无格式定义命中的文件对。\n"
      "边界：兜底选择应偏保守（默认文本比对），"
      "但必须可配置且给出「为什么用这个视图」的说明。",
      ["可配置兜底视图：文本比对（默认）/ 十六进制比对 / 图片比对 / 仅显示提示不打开",
       "兜底时可基于内容嗅探（是否含 NUL 字节、是否可 UTF-8 解码）给出建议视图并说明依据",
       "兜底决策记录在日志中，便于排查",
       "提供「为这个扩展名创建格式定义」的一键入口（从兜底场景直接进入新建格式）",
       "兜底行为不影响已定义格式的命中"],
      "BC5 §1 SESS-014 + §14.1"),

    A("FMT-010", "文件格式", "格式定义的内置库",
      "入口：内置格式定义集合。\n"
      "作用对象：开箱即用的常见格式支持。\n"
      "边界：内置定义必须覆盖最常见场景，"
      "否则用户第一步就要自己配格式。",
      ["内置定义至少覆盖：纯文本、C/C++、头文件、Python、JavaScript、JSON、XML、HTML、CSS、Markdown、INI/CONF、Shell、批处理、SQL、CSV、TSV、日志、Makefile",
       "每个内置定义声明掩码、注释语法、关键字、以及默认视图类型",
       "内置定义有单元测试：对每种格式的样例文件断言高亮与忽略注释行为",
       "内置掩码允许有意重叠（如纯文本兜底与具体语言），按 FMT-002 优先级确定命中；测试发现不可达/意外遮蔽并要求显式记录",
       "内置定义可被用户复制后修改，原定义保持只读"],
      "BC5 §14.3 + §14.5"),

    A("FMT-011", "文件格式", "格式定义的导入导出与共享",
      "入口：文件格式管理界面的导入/导出。\n"
      "作用对象：格式定义的迁移与团队共享。\n"
      "边界：导入必须是合并而不是覆盖（除非用户显式选择覆盖），"
      "且冲突必须能让用户逐个决定。",
      ["支持导出单个/多个/全部格式定义为文件",
       "导入时给出清单（新增 N、冲突 M、覆盖 K），冲突逐项让用户选择保留哪个",
       "导入不删除用户已有定义",
       "导出文件为可读文本，便于纳入版本管理",
       "导入格式为非法文件时给出明确错误且部分不生效"],
      "BC5 §37（设置存储与迁移）"),

    A("FMT-012", "文件格式", "格式定义引擎的测试",
      "入口：测试工程的格式套件。\n"
      "作用对象：定义解析、掩码命中、优先级、继承覆盖。\n"
      "边界：命中优先级是核心护栏，必须有专门的顺序测试。",
      ["语料：内置定义 + 用户定义 + 会话覆盖三层同时命中一个文件，断言最终使用的定义",
       "断言优先级顺序调整后命中结果变化符合预期",
       "断言用户覆盖内置定义时不修改内置定义文件本身",
       "断言损坏的定义文件被跳过且其余定义仍可用",
       "断言「格式查询诊断」输出的命中来源准确"],
      "Ailecium Code/Tests 约定"),

    # ------------------------------------------------------------------
    # RPT —— 报表与导出
    # ------------------------------------------------------------------
    A("RPT-001", "报表导出", "报表引擎与布局模型",
      "入口：Report 页；会话内报表命令。\n"
      "作用对象：把比对结果渲染为多种格式。\n"
      "边界：报表引擎必须与视图解耦——"
      "报表数据来自比对模型而不是从控件里抓取文本。",
      ["报表数据模型与视图无关，可脱离界面生成",
       "布局模型：摘要、并排、交错、统计四种，同一份数据支持全部布局",
       "渲染器：TXT、HTML、CSV、XML 四种，新增格式只需实现渲染器",
       "报表头部统一包含：数据源、会话设置摘要、生成时间、工具版本",
       "报表引擎有单元测试（用固定比对模型断言四种格式输出）"],
      "BC5 §16.1–16.3（报表入口、布局、选项）"),

    A("RPT-002", "报表导出", "报表输出目标",
      "入口：Report 页 Output 组。\n"
      "作用对象：报表去向。\n"
      "边界：输出到剪贴板与文件必须内容一致（除编码差异）；"
      "输出到文件必须原子写入。",
      ["支持输出到文件（带路径选择与已存在确认）、剪贴板、打印机、打印预览",
       "输出文件采用临时文件 + 原子替换，失败不留半成品",
       "剪贴板输出在内容过大（>10MB）时给出提示并建议改用文件",
       "输出路径可配置默认目录与文件名模板（含数据源名与时间戳）",
       "输出后可一键在系统中打开该文件"],
      "BC5 §16.1 + §16.5（打印）"),

    A("RPT-003", "报表导出", "报表选项（包含范围与显示项）",
      "入口：Report 页 Options 组。\n"
      "作用对象：报表内容的取舍。\n"
      "边界：默认必须「只含差异」，"
      "因为包含全部条目会让大目录报表不可用。",
      ["可选项：包含相同项、包含孤儿项、包含二进制项、包含时间戳、包含 CRC、包含文件属性",
       "链接样式可选：无链接、相对路径、file:// 绝对链接",
       "HTML 报表默认内嵌 CSS 与脚本以符合 RPT-007 单文件离线契约；不提供与之冲突的外链资源默认模式",
       "可指定每页/每文件的最大条目数并自动分页",
       "选项可保存为报表预设"],
      "BC5 §16.3（报表选项）"),

    A("RPT-004", "报表导出", "打印与页面设置",
      "入口：Report 页的打印与页面设置。\n"
      "作用对象：纸质/PDF 输出。\n"
      "边界：打印必须能正确处理超长行（折行或横向分页），"
      "不能把内容裁掉。",
      ["提供打印预览，显示分页、页眉页脚、行号连续",
       "页面设置：纸张、方向、边距、页眉页脚内容（含文件名与页码）",
       "超长行支持折行显示或缩小字号选项，不裁切内容",
       "彩色打印时可选择「白底黑字」（不打印背景色）以节省墨水并保证可读",
       "支持输出为 PDF（通过系统打印驱动或内置生成）"],
      "BC5 §16.5（打印与页面设置）"),

    A("RPT-005", "报表导出", "统计类报表",
      "入口：Report 页 Statistical 布局。\n"
      "作用对象：汇总数字与分类计数。\n"
      "边界：统计口径必须与界面状态栏、汇总面板一致，"
      "三者不一致属于缺陷。",
      ["统计内容：总条目数、各类状态计数、差异占比、总大小、按扩展名分组统计、按目录深度统计",
       "统计报表可输出为 CSV 便于后续分析",
       "统计口径与状态栏、汇总面板严格一致（有交叉测试）",
       "支持按自定义分组维度统计（如按扩展名、按修改月份）",
       "统计图表可选在 HTML 报表中以简单柱状图呈现（不引入重型图表依赖）"],
      "BC5 §16.2（统计布局）+ §9.1"),

    A("RPT-006", "报表导出", "差异摘要报表（可读性优先）",
      "入口：Report 页摘要布局。\n"
      "作用对象：给人看的差异摘要。\n"
      "边界：摘要必须能回答「这次比较最值得关注的是什么」。",
      ["摘要结论区分有差异、无差异、结果不完整/存在错误，并列出差异最多的前 N 项、最大差异和冲突数；未知不得汇总为无差异",
       "对文件夹比对给出「变化最剧烈的目录 Top N」",
       "摘要可直接粘贴到邮件或提交信息（纯文本、无多余格式）",
       "摘要与详细报表可一起输出为单个 HTML 文件",
       "摘要内容长度可控（可指定 Top N）"],
      "BC5 §16.2 + §16.4"),

    A("RPT-007", "报表导出", "HTML 报表的交互能力",
      "入口：HTML 输出。\n"
      "作用对象：浏览器中的报表体验。\n"
      "边界：HTML 报表必须单文件、离线可用、"
      "不依赖任何外部 CDN（网络受限环境下也要能打开）。",
      ["HTML 报表为单文件，CSS/脚本内嵌，不自动加载外部资源；用户路径和文件内容必须转义，不把文本中的 http 字样误当外部资源引用",
       "支持折叠/展开文件分组、按状态过滤、按关键字搜索",
       "差异行着色与界面一致，且提供「白底黑字」打印样式",
       "大报表（>10 万行）在浏览器中可流畅滚动（虚拟化或分块）",
       "报表在 Chrome / Edge / Safari 上显示一致（人工核对清单）"],
      "BC5 §16.2 + 用户环境：网络受限需离线可用"),

    A("RPT-008", "报表导出", "报表的编码与本地化",
      "入口：报表选项中的编码与语言。\n"
      "作用对象：报表文本编码与界面语言。\n"
      "边界：Windows 下用 Excel 打开 CSV 时中文乱码是高发问题，"
      "默认必须选择能避免该问题的编码。",
      ["支持输出编码：UTF-8、UTF-8 with BOM、UTF-16 LE、系统 ANSI、GBK",
       "CSV 在 Windows 下默认使用 UTF-8 with BOM 以避免 Excel 乱码（有其他平台差异时分别设默认）",
       "报表语言跟随界面语言，可单独指定",
       "报表中的时间格式遵循所选区域设置，且 ISO 8601 为可选",
       "编码与语言选项保存为报表预设"],
      "BC5 §16.3 + §35（国际化）"),

    A("RPT-009", "报表导出", "报表的性能与流式生成",
      "入口：大结果集报表生成。\n"
      "作用对象：内存占用与响应性。\n"
      "边界：报表生成必须流式写出，"
      "不能先在内存拼完整字符串再一次性落盘。",
      ["大报表采用流式写出，内存占用与结果规模不呈线性增长（有断言）",
       "生成过程中显示进度（已处理条目数/总数）并可取消",
       "取消后删除半成品文件并明确提示",
       "超大结果集（如 50 万条差异）在 60s 内完成 TXT/CSV 输出",
       "生成过程不阻塞界面（后台线程 + 取消标志）"],
      "BC5 §16 + §36（性能）"),

    A("RPT-010", "报表导出", "报表的自动化调用",
      "入口：命令行与脚本接口。\n"
      "作用对象：无人值守场景。\n"
      "边界：命令行报表生成必须支持全部关键选项，"
      "否则自动化场景只能做一半。",
      ["支持命令行指定报表格式、布局、输出路径、包含范围与过滤条件",
       "支持「只在有差异时才生成报表」的条件输出",
       "报表文件路径支持模板变量（时间戳、数据源名）",
       "生成失败时返回非零退出码并输出可解析的错误信息",
       "退出码统一引用 CLI-004（0 无差异 / 1 有差异 / 2 参数错误 / 3 数据源错误 / 4 内部错误）；报表失败不得返回成功"],
      "BC5 §16 + §29（命令行）"),

    A("RPT-011", "报表导出", "报表模板与自定义",
      "入口：报表选项中的「自定义模板」（进阶能力）。\n"
      "作用对象：非固定格式的输出需求。\n"
      "边界：模板必须沙箱化——"
      "不允许执行任意代码，只做占位符与循环替换。",
      ["支持用户提供自定义模板文件（占位符 + 简单循环语法）",
       "模板语法为纯声明式，不接受任意表达式求值（安全要求有测试）",
       "提供模板示例与语法文档，且示例用真实比对模型渲染通过（测试保证）",
       "模板渲染失败时回退到内置布局并报告错误位置",
       "模板可随报表预设一起保存与导出"],
      "BC5 §16（报表的自定义能力）"),

    A("RPT-012", "报表导出", "报表模块的测试",
      "入口：测试工程的报表套件。\n"
      "作用对象：四种格式 × 四种布局的输出正确性。\n"
      "边界：固定比对模型 + 输出快照是最有效的护栏。",
      ["用固定比对模型对 4 种格式 × 4 种布局生成报表并做快照比对",
       "断言 HTML 报表不含外部资源引用",
       "断言 CSV 编码与转义正确（含逗号、引号、换行的字段）",
       "断言剪贴板输出与文件输出内容一致（除编码）",
       "断言取消生成后不留半成品文件"],
      "Ailecium Code/Tests 约定"),

    # ------------------------------------------------------------------
    # PAT —— 补丁
    # ------------------------------------------------------------------
    A("PAT-001", "补丁", "补丁生成（diff 输出）",
      "入口：文本比对会话 → 另存为补丁 / 复制为补丁。\n"
      "作用对象：当前差异结果。\n"
      "边界：补丁格式必须是标准 unified diff，"
       "并且必须经过标准工具验证可用。",
      ["生成 unified diff，含 `---`/`+++` 头、`@@` hunk 头、上下文行与增删行",
       "支持指定上下文行数（默认 3）与路径前缀（默认 a/ b/）",
       "支持多文件补丁（文件夹比对结果整体导出为一个补丁）",
       "生成的补丁用 `git apply` 与 `patch -p1` 双双验证可应用（端到端测试）",
       "可选生成二进制补丁段（git binary patch）",
       "补丁基于原始内容生成，默认包含被显示过滤或重要性规则隐藏的实际字节变化；选择局部补丁时明确范围，不宣称其可把完整 A 还原为 B"],
      "BC5 §17（补丁文件）"),

    A("PAT-002", "补丁", "补丁应用（视角为「应用」而非「查看」）",
      "入口：补丁视图 → 应用补丁；命令行应用。\n"
      "作用对象：目标文件。\n"
      "边界：应用必须全部成功或全部回滚，"
       "不允许留下部分应用的中间状态。",
      ["应用前预演全部 hunk，列出可应用/不可应用与原因",
       "支持逐 hunk 选择应用范围",
       "应用失败时自动从备份回滚，目标文件字节级恢复原状（有测试）",
       "应用成功的 hunk 数与偏移量调整（fuzz）在报告中列出",
       "支持反向应用（撤销已应用的补丁）"],
      "BC5 §17 + EDV-003"),

    A("PAT-003", "补丁", "补丁格式兼容性",
      "入口：打开或应用补丁时。\n"
      "作用对象：不同来源的补丁文件。\n"
      "边界：兼容性必须有测试语料，不能只支持自家生成的补丁。",
      ["支持 git diff、svn diff、hg diff、GNU diff、diff -u、context diff 等常见格式",
       "支持 `\\ No newline at end of file` 标记",
       "支持 CRLF 与 LF 混合的补丁文件",
       "支持带索引行的 git 补丁（`index abc123..def456`）",
       "每种格式有对应的解析测试语料"],
      "BC5 §17"),

    A("PAT-004", "补丁", "补丁与版本控制系统集成",
      "入口：补丁视图的命令与选项。\n"
      "作用对象：在仓库中应用补丁的常见工作流。\n"
      "边界：本工具不替代版本控制系统；"
      "它只生成与消费补丁文件，仓库操作交给外部工具。",
      ["生成补丁时可指定仓库相对路径与 a/b 前缀以适配 `git apply`",
       "提供「复制 git apply 命令」快捷入口（把命令写到剪贴板）",
       "支持从剪贴板读取补丁并直接应用",
       "应用补丁时可选「同时更新索引」提示（仅提示，不直接操作仓库）",
       "与外部版本控制工具的调用通过统一的外部工具服务完成"],
      "BC5 §17 + §28（版本控制集成）"),

    A("PAT-005", "补丁", "补丁相关的安全与确认",
      "入口：应用补丁时。\n"
      "作用对象：补丁对文件系统的写操作。\n"
      "边界：补丁可包含路径穿越与文件删除等危险动作，"
      "必须做路径安全校验并强制确认。",
      ["应用前校验补丁中的目标路径不允许逃逸出指定根目录",
       "涉及删除文件或大规模改写的补丁额外确认并列出受影响文件",
       "应用前对所有受影响文件创建 .orig 备份，备份位置与保留策略可配置",
       "补丁中包含二进制改写或权限变更时单独提示",
       "全部应用动作记录在操作日志与报表中"],
      "BC5 §17 + 安全要求"),

    A("PAT-006", "补丁", "补丁模块的测试",
      "入口：测试工程的补丁套件。\n"
      "作用对象：生成、解析、应用、回滚。\n"
      "边界：往返测试与真实工具验证是核心。",
      ["往返：A→B 生成补丁 → 应用到 A → 结果与 B 逐字节一致（含 CRLF/LF/BOM 变体）",
       "解析语料：多各来源格式 + 畸形输入（断言不崩溃且报错定位准确）",
       "回滚：构造应用中途失败的用例，断言目标文件字节级恢复",
       "安全：含 `../` 路径的补丁被拒绝",
       "在可用环境下用真实 git/patch 命令做端到端验证"],
      "Ailecium Code/Tests 约定"),

    # ------------------------------------------------------------------
    # SNAP —— 快照
    # ------------------------------------------------------------------
    A("SNAP-001", "快照", "快照生成与存储",
      "入口：Tools 页或会话中的「生成快照」。\n"
      "作用对象：某个目录（或文件集合）在某一时刻的状态记录。\n"
      "边界：快照只记录元数据和可选摘要，不包含文件内容、不是备份；需要恢复文件须使用独立备份/复制流程，不扩展成本模块的隐含职责。",
      ["快照记录每个条目的相对路径、类型、大小、修改时间、属性，可选 CRC",
       "不保存文件内容；创建与打开时明确说明不能从快照恢复文件，CRC 只用于摘要比较",
       "快照文件为文本格式（可读、可 diff），带格式版本号与完整性校验",
       "生成过程可取消，取消不留半成品快照",
       "快照文件可导出/导入并支持在其它机器使用（路径以相对路径为主）"],
      "BC5 §26（快照）"),

    A("SNAP-002", "快照", "快照作为比较数据源",
      "入口：在新建会话时选择快照作为左/右数据源。\n"
      "作用对象：快照与实时目录的比较。\n"
      "边界：快照侧的写操作一律禁止；"
      "快照侧在界面中必须明确标注为「快照（只读）」。",
      ["快照可作为任一侧数据源参与文件夹比对",
       "快照侧只读，所有写命令对其置灰并说明原因",
       "快照含 CRC 时仅能执行 CRC 比较；不含摘要时只能比较元数据并显示内容未知；没有原始字节不得运行逐字节/规则比较或从快照复制文件",
       "快照可与另一快照比较",
       "快照数据源在界面与报表中明确标注为快照及快照时间"],
      "BC5 §26 + §9"),

    A("SNAP-003", "快照", "快照对比与差异报告",
      "入口：打开两个快照或快照与目录。\n"
      "作用对象：时间维度上的变化。\n"
      "边界：报告必须能回答「从快照 A 到快照 B 发生了什么变化」。",
      ["输出变化清单：新增、删除、内容变化、大小变化、时间变化、属性变化",
       "区分「新增」与「曾经存在后删除」（需要两次快照信息）",
       "生成变化摘要是可读的一句话结论（如「新增 12 个文件、删除 3 个、修改 8 个」）",
       "报告可导出为 TXT/HTML/CSV",
       "支持按变化类型过滤后再导出"],
      "BC5 §26 + §16"),

    A("SNAP-004", "快照", "快照管理",
      "入口：Tools 页 → 快照管理。\n"
      "作用对象：本地快照库。\n"
      "边界：快照不自动删除；"
      "删除必须显式操作并给出体积信息。",
      ["列出全部快照：名称、时间、数据源路径、条目数、文件体积",
       "支持重命名、删除、导出、导入、复制",
       "显示快照库总占用体积，并提供「清理旧快照」的辅助入口（列出候选并逐个确认）",
       "快照可绑定到某个同步会话作为基线",
       "快照文件损坏时明确标注并允许删除，不影响其它快照"],
      "BC5 §26 + §37（设置存储）"),

    A("SNAP-005", "快照", "快照驱动的同步基线（与 SYNC 联动）",
      "入口：同步会话设置 → 快照 Tab（见 SYNC-011）。\n"
      "作用对象：双向同步的变化判定基线。\n"
      "边界：快照缺失或损坏时必须降级而不是报错终止，"
      "并在界面明确说明当前处于保守模式。",
      ["同步会话可指定一个快照作为基线，用其判定哪一侧发生了变化",
       "每次成功同步后自动更新基线快照（可关闭）",
       "基线判定结果在预览清单「原因」列中显示（如「右侧相对基线已修改」）",
       "基线不可用（缺失/损坏/数据源不匹配）时明确提示并降级为无基线模式",
       "基线快照与同步会话的绑定关系可在界面查看与解除"],
      "BC5 §26 + §11（同步）"),

    A("SNAP-006", "快照", "快照模块的测试",
      "入口：测试工程的快照套件。\n"
      "作用对象：生成、加载、比较、损坏恢复。\n"
      "边界：损坏与版本不兼容的恢复路径必须有测试。",
      ["断言快照生成后重新加载得到相同条目集合（往返一致）",
       "断言快照与目录比较时识别新增/删除/修改/属性变化四类变化",
       "断言快照文件被截断或字段缺失时给出明确错误且不崩溃",
       "断言快照作为只读数据源时全部写命令被拒绝",
       "断言快照格式版本升级时的兼容处理（旧版本可加载）"],
      "Ailecium Code/Tests 约定"),
]
