# -*- coding: utf-8 -*-
"""版本控制（VCS）、命令行（CLI）、脚本自动化（SCR）、选项外观（OPT）、平台性能（PLAT）规格。"""

from . import A

ACTIONS = [
    # ------------------------------------------------------------------
    # VCS —— 版本控制集成
    # ------------------------------------------------------------------
    A("VCS-001", "版本控制", "版本控制后端抽象层",
      "入口：Views 层的版本控制视图与 Services 层的版本控制后端接口。\n"
      "作用对象：仓库探测、状态查询、修订读取的统一抽象。\n"
      "边界：视图层不得直接调用 git 命令行；"
      "全部通过后端接口，且后端可替换以便无仓库环境下降级。",
      ["定义后端接口：detectRepo、status、log、diff、blame、revisionGraph、catFile",
       "首个实现调用用户安装的外部 git 进程，避免把仓库访问库与当前构建绑定；任何新增库依据 ENG-013 单独核验许可，不凭库名称推断许可结论",
       "无 git 可执行文件时功能优雅降级（VCS 相关命令置灰并说明「未检测到 git」）",
       "仓库探测结果按路径缓存，路径变化时失效",
       "后端接口可被假实现替换，使 VCS 视图可在无 git 环境下测试"],
      "TGIT §B（TortoiseGit 各对话框）+ Ailecium Services/Git（15 文件）"),

    A("VCS-002", "版本控制", "与 HEAD 比对",
      "入口：Home 页 Source Control 组的「与 HEAD 比对」。\n"
      "作用对象：工作副本文件与仓库中 HEAD 版本的差异。\n"
      "边界：不得静默改写工作副本；"
       "比对只读，写回需显式命令。",
      ["可对单个文件或整个目录执行「与 HEAD 比对」",
       "目录场景下先展示变更文件清单（新增/修改/删除/重命名/冲突），再逐个或批量打开文件比对",
       "二进制文件自动切换到十六进制比对",
       "仓库无 HEAD（新仓库）时给出明确提示",
       "比对视图标题标明「工作副本 ↔ HEAD」与修订号"],
      "TGIT §B（TortoiseGit → Diff）+ §C.3 C018"),

    A("VCS-003", "版本控制", "比较两个修订",
      "入口：Source Control 组的「比较两个修订」；通过修订选择器。\n"
      "作用对象：仓库中任意两个提交之间的差异。\n"
      "边界：修订选择必须支持哈希前缀、分支名、标签名、相对引用（HEAD~2）等全部 git 语法。",
      ["提供修订选择器：可输入引用、可从日志列表选择、可浏览分支与标签",
       "支持文件级与目录级比较；目录级先出变更清单",
       "支持同一文件跨两个修订的内容比对",
       "无效引用给出明确错误（区分「不存在」与「歧义」）",
       "支持比较工作副本与任意修订（三方：工作副本 ↔ 修订 ↔ 可选祖先）"],
      "TGIT §B（Compare revisions）+ §C.3 C019"),

    A("VCS-004", "版本控制", "比较两个分支或标签",
      "入口：Source Control 组的「比较两个分支」；引用浏览器双选。\n"
      "作用对象：两个引用之间的差异集合。\n"
      "边界：直接比较分支头与比较某分支自共同祖先的改动是不同问题，两者都有效；界面必须清楚显示两端提交及方向，不把直接比较称为伪差异。",
      ["提供引用浏览器列出本地分支、远程分支、标签，支持过滤与多选",
       "提供「直接比较左/右分支头」与「共同祖先→右侧分支改动」两种模式，默认直接比较；显示解析后的提交哈希与所用 merge-base",
       "切换 merge-base 模式显式指定目标分支；无共同祖先或多个合并基时报告并要求选择策略，不静默猜测",
       "目录级结果以文件夹比对视图呈现，文件级可进入子视图",
       "结果可导出为变更清单与报表"],
      "TGIT §B（Compare two branches）+ §C.3 C020"),

    A("VCS-005", "版本控制", "工作副本状态列",
      "入口：文件夹比对会话中启用「版本控制状态」列。\n"
      "作用对象：每个文件在工作副本中的状态。\n"
      "边界：状态必须来自后端查询结果的批量获取（而非逐文件调用），"
      "否则大目录下会成为性能灾难。",
      ["列显示状态：未跟踪、已修改、已暂存、已删除、已重命名、冲突、忽略",
       "状态以图标与着色呈现，可排序可按状态过滤",
       "状态批量获取（一次 git status 覆盖整个仓库），并有性能断言",
       "目录节点聚合子项状态（显示「含 N 个已修改」）",
       "仓库根之外的条目状态列显示为空并说明原因"],
      "TGIT §B（工作副本状态）+ BC5 §28（VCS 集成）"),

    A("VCS-006", "版本控制", "提交日志对话框",
      "入口：Source Control 组的「显示日志」。\n"
      "作用对象：仓库提交历史列表。\n"
      "边界：对话框必须能在大型仓库（10 万提交）上流畅打开，"
      "因此必须分页/增量加载而不是一次取全部。",
      ["左侧为提交列表：修订号、作者、日期、消息首行",
       "右侧为选中提交的详细信息（完整消息、作者邮箱、父提交、变更文件列表）",
       "分页/增量加载，滚动到底部加载更多；加载过程显示占位与进度",
       "支持按路径过滤（只看影响某文件/目录的提交）",
       "双击变更文件直接打开比对视图"],
      "TGIT §B（Log Messages 对话框）"),

    A("VCS-007", "版本控制", "日志的图形列与引用徽标",
      "入口：日志对话框的 Graph 列开关。\n"
      "作用对象：提交的图形化拓扑与引用标记。\n"
      "边界：图形绘制必须能承受大量分支；"
       "分页加载时图形不能断裂或错位。",
      ["Graph 列绘制提交图的节点与连线，区分主线与分支",
       "引用徽标显示分支名、标签名、远程引用、HEAD，且各有语义色",
       "徽标过多时折叠为「+N」并可展开查看",
       "分页加载时图形与上一页正确衔接（父子连线不丢失）",
       "图形列可整体关闭以获得更紧凑列表"],
      "TGIT §B（Log 的图列与 10 余种分支徽标语义色）"),

    A("VCS-008", "版本控制", "日志过滤与搜索",
      "入口：日志对话框顶部的过滤区。\n"
      "作用对象：提交列表的筛选。\n"
      "边界：过滤是查询层能力（尽量交给 git 而非本地全量拉取后过滤），"
       "否则大仓库无法使用。",
      ["按作者、日期区间、消息关键字、路径、修订区间过滤",
       "支持正则与大小写敏感选项，正则带灾难性回溯保护",
       "过滤条件组合后生成底层查询命令，并在界面可查看实际执行的命令",
       "支持「只显示影响指定路径的提交」并与其它条件组合",
       "过滤结果计数显示（命中 N 条）"],
      "TGIT §B（Log 过滤）+ BC5 §13（过滤思路）"),

    A("VCS-009", "版本控制", "日志中的变更文件列表",
      "入口：日志对话框右下的文件列表。\n"
      "作用对象：选中提交改动的文件清单。\n"
      "边界：必须区分「相对父提交」与「相对工作副本」两种语义。",
      ["列出变更类型（新增/修改/删除/重命名/复制/权限变更）、旧路径、新路径、增删行数",
       "重命名显示为「旧 → 新」并可通过开关拆为删除+新增",
       "可按变更类型与路径过滤文件列表",
       "文件列表支持全选与批量操作（批量打开比对、批量导出）",
       "文件列表可导出为清单"],
      "TGIT §B（Log 的变更文件列表）"),

    A("VCS-010", "版本控制", "从日志打开比对",
      "入口：日志中双击文件/右键「与父提交比较」「与工作副本比较」。\n"
      "作用对象：历史版本的内容比对。\n"
      "边界：历史版本内容需从仓库读取（不落盘到工作副本），"
       "因此比对视图必须支持「虚拟数据源」。",
      ["双击文件打开该提交与该提交父版本之间的比对",
       "支持与工作副本、与任意其它修订比较",
       "历史版本内容从仓库读取，不写工作副本，不修改仓库",
       "比对视图标题标明两个版本来源（如 `abc1234^ ↔ abc1234`）",
       "支持在日志与比对视图之间往返而保留定位"],
      "TGIT §B（双击打开比对）+ BC5 §28"),

    A("VCS-011", "版本控制", "修订图（Revision Graph）",
      "入口：Source Control 组的「修订图」。\n"
      "作用对象：分支与合并的拓扑可视化。\n"
      "边界：图形布局必须对大规模历史可用（可限制深度与范围），"
       "不追求一次画出全部提交。",
      ["节点表示提交或分支头，连线表示父子与合并关系",
       "区分主线、分支线、合并线，并为不同分支分配区分色",
       "支持缩放、平移、按分支/作者/日期高亮",
       "可限制显示范围（最近 N 个提交 / 影响某路径的提交）",
       "支持导出为 SVG 与 PNG"],
      "TGIT §B（Revision Graph，可导出 SVG/PNG）"),

    A("VCS-012", "版本控制", "逐行追溯（Blame）",
      "入口：Source Control 组的「追溯」。\n"
      "作用对象：文件每一行的最后修改者信息。\n"
      "边界：追溯结果必须可导航到对应提交；"
       "对超大文件需后台计算并可取消。",
      ["显示每行的修订号、作者、日期，并在行首以窄栏着色呈现",
       "支持「忽略空白改动」与「跨重命名追溯」选项",
       "支持指定修订范围（追到某个提交为止）",
       "点击某行可跳转到对应提交的日志视图",
       "后台计算并显示进度，大文件可取消"],
      "TGIT §B（Blame）+ §C.3 C022"),

    A("VCS-013", "版本控制", "追溯视图的着色与交互",
      "入口：Blame 视图的显示选项。\n"
      "作用对象：追溯结果的视觉呈现。\n"
      "边界：着色必须保证相邻块可区分（不能只靠相似色）。",
      ["按作者着色、按日期龄连续着色（越旧越深）、按提交块着色三种模式",
       "悬停显示该行的完整提交信息（修订、作者、日期、消息首行）",
       "同一提交的连续行合并为块，块内可折叠",
       "提供「只看某作者」与「只看向某提交的区间」的过滤",
       "着色模式下仍保持文本可读性（对比度校验）"],
      "TGIT §B（Blame 按龄连续着色）"),

    A("VCS-014", "版本控制", "提交前变更审阅与外部提交工具入口",
      "入口：Source Control 组的「审阅工作区变更」。\n"
      "作用对象：待审阅的工作副本/索引差异。\n"
      "边界：只负责选择、查看和导出变更；提交、暂存、推送、变基由外部 Git 客户端完成，本工具不执行 git commit。",
      ["列出工作副本与索引的变更文件（含未跟踪），可按状态分组过滤",
       "勾选只决定本次审阅/导出范围，不改变暂存区，不声称决定实际提交内容",
       "双击打开工作副本↔索引或工作副本↔HEAD 的只读差异预览",
       "提供复制变更清单与打开已配置外部提交工具入口，参数交由统一外部工具服务",
       "外部工具退出后刷新状态；本工具不存储提交消息、不执行暂存或提交，自动化测试验证仓库索引与引用未被审阅改动"],
      "TGIT §B（Commit 对话框）"),

    A("VCS-015", "版本控制", "冲突解决工作流",
      "入口：工作副本存在冲突时从状态视图/提交视图进入。\n"
      "作用对象：冲突文件的三方合并。\n"
      "边界：必须使用仓库提供的三个版本（ours / base / theirs）作为三方合并输入，"
       "不允许自行猜测基线。",
      ["识别冲突文件并列出其三个版本（ours / base / theirs）与合并目标路径",
       "双击冲突文件打开三方合并会话，输入正确绑定到三个版本",
       "合并结果写回 MERGED 后向调用方返回状态，不自动暂存；用户可复制 git add 命令或打开外部客户端标记解决，并明确该步骤由外部工具完成",
       "提供「取我方」「取他方」「取基线」的批量快捷操作并二次确认",
       "冲突解决全过程记录在操作日志"],
      "TGIT §B（冲突解决）+ MRG-016（mergetool 契约）"),

    A("VCS-016", "版本控制", "外部版本控制工具的协作",
      "入口：Tools 页与 VCS 选项。\n"
      "作用对象：把仓库操作交给用户熟悉的工具（如 TortoiseGit、命令行）。\n"
      "边界：外部工具调用统一走外部工具服务；"
       "路径与命令必须正确转义。",
      ["提供「用外部工具打开仓库/文件历史」的自定义命令模板",
       "提供「复制 git 命令」把等价命令写入剪贴板（便于在终端执行）",
       "可选集成 TortoiseGit 菜单（仅 Windows，若已安装则提供入口）",
       "外部工具不可用时命令置灰并说明",
       "提供配置片段生成器（把本工具注册为 git difftool/mergetool 的配置）"],
      "TGIT §B（资源管理器右键菜单）+ §D（命令行参数）"),

    A("VCS-017", "版本控制", "多仓库、子模块与工作树",
      "入口：打开位于多仓库工作区/含子模块的路径。\n"
      "作用对象：嵌套仓库结构。\n"
      "边界：必须避免把子模块内部条目错误归属到父仓库，"
       "否则状态列会全错。",
      ["检测路径所属仓库；同时存在嵌套仓库时按最深匹配归属",
       "子模块条目显示父仓库中的子模块状态（有改动/未初始化）并可进入子模块仓库视图",
       "支持 git worktree（多个工作树共享同一仓库）的正确识别",
       "状态列按归属仓库分别着色并可显示归属信息",
       "嵌套场景有专门的测试语料"],
      "TGIT §B（子模块支持）"),

    A("VCS-018", "版本控制", "版本控制集成的配置与降级",
      "入口：选项 → 版本控制。\n"
      "作用对象：后端选择与可用性。\n"
      "边界：任何 VCS 功能不可用时必须明确降级提示，"
       "不允许静默失效或报出面向开发者的堆栈错误。",
      ["可配置 git 可执行文件路径（默认从 PATH 查找）、超时、并行度",
       "未检测到 git 时全部 VCS 功能置灰，tooltip 说明「未检测到 git 可执行文件」并给出配置入口",
       "git 版本过低时提示最低支持版本并降级不可用功能",
       "仓库查询失败（损坏、权限、网络盘）时给出可读错误与原始输出",
       "VCS 相关功能可整体关闭（选项），关闭后界面中不显示 VCS 相关页面与命令"],
      "Ailecium Services/Git（含 TortoiseGit 集成）+ BC5 §28"),

    A("VCS-019", "版本控制", "版本控制模块的测试",
      "入口：测试工程的 VCS 套件。\n"
      "作用对象：后端解析、视图呈现、冲突工作流。\n"
      "边界：测试用临时仓库自建历史，"
       "不依赖外部仓库网络访问。",
      ["在临时目录用 git init 构造固定历史（含分支、标签、合并、重命名、冲突）",
       "断言 status / log / diff / blame 解析结果与期望一致",
       "断言无 git 环境下全部 VCS 功能降级提示正确且不崩溃",
       "断言冲突工作流绑定正确的 ours/base/theirs",
       "真实 Git 集成用例在无 git 环境下单独 skip 并说明原因；假后端解析、降级与视图逻辑测试仍运行"],
      "Ailecium Code/Tests + TGIT §B"),

    # ------------------------------------------------------------------
    # CLI —— 命令行
    # ------------------------------------------------------------------
    A("CLI-001", "命令行", "命令行入口与参数解析",
      "入口：可执行文件带参数运行。\n"
      "作用对象：全部命令行参数。\n"
      "边界：参数解析必须有统一实现与自动生成的帮助文本，"
       "避免出现「文档里有但代码不支持」的参数。",
      ["原生位置参数：1 个=单文件/目录、2 个=左右文件/目录、3 个=左/右/祖先、4 个=左/右/祖先/输出；Git mergetool 用命名参数明确绑定，不能另猜次序",
       "各平台支持 -- 开关与 -- 参数终止符；Windows 兼容已知 / 开关，POSIX 绝对路径不得被误解析为 / 开关；参数识别与路径消歧有测试",
       "提供 /? 与 --help 输出完整参数说明，内容由参数表自动生成",
       "无法识别的参数给出明确错误与「最接近的候选参数」建议，退出码非零",
       "参数解析为纯函数并有完整单元测试（含引号、空值、重复参数）"],
      "BC5 §29.2（命令行参数）+ TGIT §D（TortoiseGitProc）"),

    A("CLI-002", "命令行", "会话类型与视图选择开关",
      "入口：命令行 `/fv=\"Text Compare\"` 风格开关。\n"
      "作用对象：绕过自动视图判定直接指定视图。\n"
      "边界：指定的视图类型必须校验是否适用于给定的参数个数，"
       "不适用时报错而不是打开空视图。",
      ["可指定注册表中全部适用会话类型，包括文本比较、文本合并、文本编辑、文本补丁及文件夹/数据类；列表由 SESS-002 生成，平台或实现不可用时说明原因",
       "视图类型名称支持别名与大小写不敏感匹配",
       "类型与参数个数不匹配时给出明确错误（如文件夹类型收到两个文件）",
       "未指定类型时走自动判定（SESS-013）",
       "可列出全部可用类型（`--list-session-types`）以支持脚本生成"],
      "BC5 §29.2 + TGIT §D"),

    A("CLI-003", "命令行", "比较规则与选项的命令行覆盖",
      "入口：命令行开关。\n"
      "作用对象：本次运行的比较规则。\n"
      "边界：命令行开关只影响本次运行，"
       "绝不写入会话默认值（这一条必须有测试）。",
      ["支持忽略空白、忽略大小写、忽略行尾、忽略注释等规则开关",
       "支持指定编码、行尾、对齐样式、容差等参数",
       "支持指定过滤掩码与排除掩码",
       "支持 `/silent`（静默）与 `/qc`（快速比较，只返回结果不显示界面）等自动化开关",
       "断言：带开关运行后用户配置文件与会话默认值未被修改"],
      "BC5 §29.3（命令行开关）+ §30（脚本）"),

    A("CLI-004", "命令行", "静默模式与返回码语义",
      "入口：`/silent`、`/qc` 等。\n"
      "作用对象：无人值守场景的可用性。\n"
      "边界：返回码是自动化集成的契约，"
       "一经发布不可改变语义。",
      ["比较模式返回码：0=无差异、1=有差异、2=参数或使用错误、3=无法读取/写入数据源或输出、4=内部错误；mergetool 模式 0=已解决且成功保存，1=未解决或用户取消，其余错误沿用此表",
       "返回码语义有稳定性测试（常量被快照锁定）",
       "静默模式不显示任何界面，全部输出走标准输出/错误",
       "静默模式下仍生成日志文件（路径可配置）",
       "提供 `--print-exit-codes` 输出返回码表便于脚本使用"],
      "BC5 §29.4（返回码）+ §30.3"),

    A("CLI-005", "命令行", "只读与只写侧控制",
      "入口：`/leftreadonly`、`/rightreadonly` 等开关。\n"
      "作用对象：防止自动化脚本误改数据。\n"
      "边界：只读开关必须在全部写入路径上生效，"
       "且有测试覆盖（不只是界面置灰）。",
      ["支持分别将左侧、右侧或双侧设为只读",
       "只读在模型层生效：任何写入操作被拒绝并记录日志，而不是仅界面置灰",
       "与 `/silent` 组合时若无写权限导致任务失败，返回码反映该失败",
       "只读状态在界面（非静默模式）中显示在状态栏",
       "有测试：构造写入尝试并断言被模型层拒绝"],
      "BC5 §29.3（/leftreadonly）"),

    A("CLI-006", "命令行", "输出重定向与日志",
      "入口：`--log`、`--report` 等参数。\n"
      "作用对象：自动化流水线的输出采集。\n"
      "边界：标准输出必须是「可解析的稳定格式」，"
       "不能混入欢迎语与进度条符号。",
      ["支持把比较结果摘要写入指定文件（文本/CSV 可选）",
       "支持把日志写入指定文件，日志级别可指定",
       "标准输出在静默模式下只输出结果摘要（每行一条，字段以制表符分隔）",
       "支持 `--json` 输出结构化结果便于脚本消费",
       "输出编码固定为 UTF-8，避免跨平台脚本解析问题"],
      "BC5 §29.5（命令行的其它能力）+ §16"),

    A("CLI-007", "命令行", "命令行调用已运行实例",
      "入口：已有实例运行时再次带参数启动。\n"
      "作用对象：单实例行为与窗口复用。\n"
      "边界：必须能通过选项切换「单实例复用」与「多实例独立」，"
       "因为自动化场景常需要多实例并行。",
      ["默认单实例：新参数转发到已有实例并在其中新建会话，本进程退出",
       "提供 `--new-instance` 强制启动独立实例",
       "提供 `--wait` 让调用方等待被处理完再返回（自动化场景常用）",
       "转发失败（IPC 不可用）时自动降级为启动新实例并记录日志",
       "进程间通信的实现有超时保护，不会让调用方无限等待"],
      "BC5 §29.5 + PLAT-006（单实例）"),

    A("CLI-008", "命令行", "命令行报表生成",
      "入口：`--report=html --report-file=...`。\n"
      "作用对象：CI 场景的产物生成。\n"
      "边界：必须能在无界面环境下完成（不依赖创建窗口）。",
      ["支持指定报表格式、布局、输出路径、包含范围",
       "支持「只在有差异时生成报表」（`--report-on-diff-only`）",
       "报表文件路径支持模板变量（时间戳、数据源名）",
       "报表生成失败时返回码非零并输出可解析错误",
       "在无界面环境（无 DISPLAY / offscreen 平台）下报表生成可用（有测试）"],
      "BC5 §29 + §16（报表）"),

    A("CLI-009", "命令行", "脚本文件引用",
      "入口：`@script.txt` 或 `--script=file`。\n"
      "作用对象：批量操作序列。\n"
      "边界：脚本文件的路径解析必须相对于脚本自身而非工作目录，"
       "否则脚本可移植性极差。",
      ["支持通过参数指定一个脚本文件并顺序执行其中的命令",
       "脚本内引用的相对路径相对于脚本文件所在目录解析",
       "脚本执行失败时明确报出脚本行号与失败命令",
       "脚本可通过 `--continue-on-error` 决定是否继续后续命令",
       "脚本执行的完整日志可输出到文件"],
      "BC5 §30（脚本语言）+ SCR 模块"),

    A("CLI-010", "命令行", "路径参数与通配展开",
      "入口：命令行传入含通配符的路径。\n"
      "作用对象：参数中的 `*`、`?` 等字符。\n"
      "边界：Windows 上 shell 不展开通配（由程序负责），"
       "Unix 上 shell 会展开（程序不能再展开），必须区分处理。",
      ["Windows 上由程序展开通配符；Unix 上沿用 shell 展开结果，不重复展开",
       "通配展开结果排序稳定（按路径字典序）",
       "通配匹配到 0 项时给出明确错误（区分「未找到」与「路径不存在」）",
       "展开数量不满足会话类型参数个数时返回参数错误并列出数量；不得截取前两个文件后继续比较或写入",
       "路径含空格、Unicode、引号时解析正确（有测试）"],
      "BC5 §29.2 + §13（掩码）"),

    A("CLI-011", "命令行", "命令行的帮助与发现能力",
      "入口：`--help`、`--help-session-types` 等。\n"
      "作用对象：用户与脚本作者的可用性。\n"
      "边界：帮助文本必须由代码中的参数表生成，"
       "避免文档与实现漂移（有测试断言）。",
      ["`--help` 输出按功能分组的完整参数表",
       "支持 `--help=<参数名>` 查询单个参数的详细说明与示例",
       "帮助文本生成自参数定义表，且测试断言「每个已注册参数都出现在帮助中」",
       "帮助输出不依赖界面，在无 GUI 环境下可用",
       "帮助文本使用界面当前语言，可指定 `--lang`"],
      "BC5 §29 + §38（帮助）"),

    A("CLI-012", "命令行", "命令行模块的测试",
      "入口：测试工程的命令行套件。\n"
      "作用对象：参数解析、返回码、静默模式、脚本引用。\n"
      "边界：以真实进程调用做端到端断言，而不只测解析函数。",
      ["解析单元测试：全部参数、别名、错误用法、缺值、重复值",
       "端到端测试：真实启动进程并断言返回码与标准输出格式",
       "断言静默模式不创建窗口（用 offscreen 平台并断言无窗口对象）",
       "断言命令行开关不修改用户配置（运行前后配置文件字节比对）",
       "断言 `--wait` 语义正确（调用方等待到处理完成）"],
      "Ailecium Code/Tests 约定"),

    # ------------------------------------------------------------------
    # SCR —— 脚本与自动化
    # ------------------------------------------------------------------
    A("SCR-001", "脚本自动化", "脚本引擎与脚本语法",
      "入口：`--script` 参数或脚本文件双击运行。\n"
      "作用对象：可重复执行的比较/同步操作序列。\n"
      "边界：脚本语言必须可静态检查（执行前发现语法错误），"
       "不允许只在运行到那一行时才报错。",
      ["脚本为一行一条命令的文本格式，支持注释与空行",
       "提供执行前的语法与命令名校验，一次性列出全部错误（含行号）",
       "命令名与参数解析复用命令行参数表，保证语义一致",
       "脚本引擎不依赖界面，可在无 GUI 环境运行",
       "脚本文件编码支持 UTF-8 与 BOM，行尾兼容 CRLF/LF"],
      "BC5 §30.1（脚本机制）"),

    A("SCR-002", "脚本自动化", "脚本命令：数据源与比较",
      "入口：脚本命令 load / compare / select。\n"
      "作用对象：会话与数据源的加载及比较执行。\n"
      "边界：脚本中的路径必须能用变量表达，"
       "以便同一脚本复用于不同目录。",
      ["提供命令：load（加载会话/文件/文件夹）、compare（执行比较）、select（按条件选择条目）",
       "比较命令支持指定视图类型与规则开关，语义与命令行一致",
       "支持指定输出（`log`、`print`）以便脚本产生可读结果",
       "命令返回状态可被后续命令查询",
       "全部命令有参数与示例文档，且文档由命令表生成"],
      "BC5 §30.2（脚本命令：共 29 个）"),

    A("SCR-003", "脚本自动化", "脚本命令：文件操作与同步",
      "入口：脚本命令 copy / move / delete / rename / sync / mirror。\n"
      "作用对象：脚本驱动的文件系统操作。\n"
      "边界：脚本中的破坏性操作必须有 `--dry-run` 预演能力，"
       "并且默认走回收站（与交互行为一致）。",
      ["提供命令：copy、move、delete、rename、mkdir、sync、mirror、applypatch",
       "破坏性命令先生成同源操作计划，--dry-run 只输出不执行；静默执行须显式执行参数并指定冲突/删除策略，需交互而未授权时失败，不默认回答确认框",
       "删除默认走回收站；永久删除需显式参数",
       "操作结果输出为可解析格式（每行一条记录，含结果与失败原因）",
       "脚本中的文件操作同样写入会话操作日志"],
      "BC5 §30.2 + §10（文件操作）"),

    A("SCR-004", "脚本自动化", "脚本变量与参数",
      "入口：脚本运行时的参数传入与变量定义。\n"
      "作用对象：脚本的可复用性。\n"
      "边界：变量展开必须有转义机制，"
       "否则路径中含 `$` 或 `%` 时会出错。",
      ["支持从命令行向脚本传参（`--script-arg name=value`）",
       "支持脚本内定义变量并在后续命令中引用（`${name}`）",
       "支持内置变量：脚本目录、当前目录、时间戳、工具版本、上次比较结果",
       "提供转义机制表达字面量的 `$` 与 `%`，并有测试",
       "未定义变量引用时在执行前报错并指出行号"],
      "BC5 §30.1 + §30.2"),

    A("SCR-005", "脚本自动化", "脚本控制流与条件",
      "入口：脚本命令 if / endif / goto / label。\n"
      "作用对象：按比较结果决定后续动作。\n"
      "边界：控制流必须限制跳转范围（前向或同块内），"
       "禁止任意跳转导致不可终止的脚本（或提供执行步数上限保护）。",
      ["支持条件判断（基于上次命令结果、比较是否有差异、文件是否存在）",
       "支持 label + goto 与 if/endif 块结构",
       "提供执行步数上限与超时保护，超限时终止并明确报告",
       "支持 loop/foreach 遍历选择集或文件列表",
       "控制流有完整测试（含嵌套与边界）"],
      "BC5 §30.2（脚本控制流）"),

    A("SCR-006", "脚本自动化", "脚本错误处理与返回码",
      "入口：脚本执行过程中的错误。\n"
      "作用对象：脚本的可靠性。\n"
      "边界：默认行为必须保守——"
       "遇到错误默认终止脚本并返回非零码，而不是默默继续。",
      ["默认遇错终止脚本，输出脚本行号、命令、错误信息与退出码",
       "提供 `--continue-on-error` 改为继续执行并汇总全部错误",
       "提供命令级 `on-error=` 参数细化处理（终止/跳过/重试一次）",
       "脚本最终返回码遵循 CLI-004 的定义，并可与脚本内显式设置的返回码合并（取最严重）",
       "错误输出格式稳定可解析"],
      "BC5 §30.3（脚本错误处理与返回码）"),

    A("SCR-007", "脚本自动化", "脚本的日志与审计",
      "入口：脚本运行日志。\n"
      "作用对象：无人值守任务的事后追溯。\n"
      "边界：日志必须能回答「哪一步做了什么、结果如何」，"
       "并且不包含凭据等敏感信息。",
      ["日志包含：时间、脚本行号、命令、参数、结果、耗时",
       "日志可输出到文件并支持追加与轮转",
       "敏感参数（密码、令牌）在日志中被掩码",
       "日志级别可配置（摘要/详细/调试）",
       "脚本执行结束输出汇总（成功步数/失败步数/总耗时）"],
      "BC5 §30 + §38（诊断）"),

    A("SCR-008", "脚本自动化", "脚本调度与定时执行",
      "入口：操作系统计划任务 / 内部调度（可选能力）。\n"
      "作用对象：周期性比较任务。\n"
      "边界：本工具不实现常驻服务；"
       "调度交由操作系统，本工具只提供「可被调度」的脚本入口与日志。",
      ["提供「生成计划任务命令」的助手（输出可在自启动脚本中直接使用）",
       "脚本支持「若上次运行实例仍在进行则跳过本次」（锁文件机制）",
       "锁文件残留（上次异常退出）时能被识别并自动清理，且有日志",
       "提供脚本运行的并发保护说明与配置项",
       "文档给出 Windows 计划任务与 cron 的配置示例"],
      "BC5 §31（外部调用与自动化）+ 用户场景：大量无人值守任务"),

    A("SCR-009", "脚本自动化", "脚本的录制与生成（辅助能力）",
      "入口：Tools 页的「开始录制」/「生成脚本」。\n"
      "作用对象：把交互操作转成可复用脚本。\n"
      "边界：录制是辅助能力，生成的脚本必须可读可编辑，"
       "不生成不可维护的机器码式脚本。",
      ["录制会话创建、规则设置、比较、导出报表等操作并生成脚本文本",
       "生成的脚本包含注释说明每一步的由来",
       "不录制只影响视图显示的操作（缩放、滚动）以避免噪声",
       "录制的脚本可立即执行以验证等价性",
       "支持「把当前会话导出为脚本」的一键入口"],
      "BC5 §30.1 + 用户偏好：以实操驱动并希望可复用"),

    A("SCR-010", "脚本自动化", "脚本模块的测试",
      "入口：测试工程的脚本套件。\n"
      "作用对象：解析、执行、控制流、错误处理、变量。\n"
      "边界：以临时目录 + 真实文件构造端到端场景。",
      ["断言语法检查一次性报出全部错误及行号",
       "断言控制流（if/goto/loop）的嵌套与边界行为",
       "断言破坏性命令的 `--dry-run` 不改变文件系统（前后目录树快照比对）",
       "断言脚本超时与步数上限保护生效",
       "断言脚本返回码与 CLI-004 定义一致"],
      "Ailecium Code/Tests 约定"),

    # ------------------------------------------------------------------
    # OPT —— 选项与外观
    # ------------------------------------------------------------------
    A("OPT-001", "选项外观", "程序选项对话框框架",
      "入口：Application Menu → 选项；Tools 页 Options 组。\n"
      "作用对象：全部程序级设置。\n"
      "边界：选项对话框必须支持搜索——"
       "设置项数量大，逐页翻找不可接受。",
      ["左侧分类树（常规、比较、显示、文件操作、文件夹视图、文本编辑、报表打印、版本控制、网络、日志诊断、自定义命令、快捷键）",
       "顶部提供设置搜索框，输入关键字直接定位到设置项并高亮",
       "底部提供确定/取消/应用；修改未应用时切换分类给出提示",
       "每个设置项有 tooltip 说明其作用与默认值",
       "设置项的读写集中在设置仓库中，界面不直接访问配置文件"],
      "BC5 §32（程序选项）+ §38"),

    A("OPT-002", "选项外观", "常规选项",
      "入口：选项 → 常规。\n"
      "作用对象：应用启动与实例行为。\n"
      "边界：启动行为变更必须立即生效或明确提示需重启。",
      ["启动行为：记忆上次会话 / 打开 Home 页 / 打开指定工作区",
       "单实例：复用已有实例（默认）/ 允许多实例",
       "关闭最后一个会话时可选择退出或返回 Home 页，不保留没有入口的空白中央区域",
       "随系统启动（注册自启动项，可关闭）",
       "文件关联与右键菜单集成（Windows 可注册，提供开启/关闭）"],
      "BC5 §32（常规选项）+ TGIT §B（Shell 集成）"),

    A("OPT-003", "选项外观", "比较规则的默认值选项",
      "入口：选项 → 比较。\n"
      "作用对象：新建会话的默认比较规则。\n"
      "边界：程序选项里的比较默认值是 SESS-015 的统一入口，写入同一份会话类型默认存储；不得额外引入与 SESS-007 冲突的第五层覆盖。",
      ["提供各类会话的默认规则的统一入口（文本/文件夹/表格/图片/媒体）",
       "提供「恢复某一类为出厂默认」与「全部恢复出厂默认」",
       "显示每项来源：视图 / 会话 / 类型默认 / 出厂默认；程序选项中编辑类型默认与 SESS-015 指向同一数据",
       "修改在新建会话时生效，已打开会话不受影响（明确提示）",
       "导入/导出复用会话类型默认值格式与设置仓库，不创建单独的程序比较默认值副本"],
      "BC5 §32（Comparison 相关选项）+ SESS-007（作用域）"),

    A("OPT-004", "选项外观", "显示与外观选项",
      "入口：选项 → 显示。\n"
      "作用对象：字体、颜色、图标、界面元素。\n"
      "边界：字体设置必须区分「界面字体」与「内容字体」（等宽/比例），"
       "二者语义不同不可混用。",
      ["界面字体与大小可设置（影响 Ribbon、菜单、列表）",
       "内容字体与大小可设置（文本视图使用，默认等宽）",
       "语法高亮与差异着色的颜色可逐项自定义，并有预设方案",
       "图标风格选择（彩色/单色）与图标大小档位",
       "全部外观设置支持恢复默认与导出/导入主题文件"],
      "BC5 §33（外观：颜色/字体/主题）"),

    A("OPT-005", "选项外观", "文件操作选项",
      "入口：选项 → 文件操作。\n"
      "作用对象：复制、移动、删除、覆盖策略的默认行为。\n"
      "边界：安全相关默认值必须保守（默认走回收站、默认不覆盖）。",
      ["删除方式默认：回收站（默认）/ 永久删除；并提示永久删除不可恢复",
       "覆盖策略默认：询问 / 覆盖 / 跳过；并区分「文件较新」的特殊提示",
       "复制时默认保留的元数据项（时间戳/属性/权限）",
       "大文件操作前的体积确认阈值（默认 100MB）与批量删除条数阈值（默认 20）",
       "操作后可选的校验方式（无/大小/CRC）"],
      "BC5 §10 + §32 + 用户要求：默认保守、破坏性操作先确认"),

    A("OPT-006", "选项外观", "文件夹视图选项",
      "入口：选项 → 文件夹视图。\n"
      "作用对象：文件夹比对的默认显示行为。\n"
      "边界：影响性能的默认值必须偏向保守（大目录不自动展开）。",
      ["默认列集合与列宽、默认排序方式与「文件夹优先」开关",
       "默认展开策略（不展开/仅差异）与自动后台扫描开关",
       "大目录阈值（超过则不自动展开并提示）与并行扫描线程数",
       "默认显示过滤器与上下文行数",
       "双击行为（打开子视图 / 在新标签打开 / 用系统程序打开）"],
      "BC5 §9（文件夹视图）+ §8.3 + §32"),

    A("OPT-007", "选项外观", "文本编辑与视图选项",
      "入口：选项 → 文本编辑。\n"
      "作用对象：编辑器与文本视图的默认行为。\n"
      "边界：制表符与缩进的默认值必须同时用于显示与编辑，"
       "不能显示一套、插入另一套。",
      ["Tab 宽度与「Tab 转空格」默认设置（含「按语言自动」的规则表）",
       "自动换行默认模式、行号栏默认显隐、空白字符默认显隐",
       "自动缩进、括号匹配高亮、当前行高亮等编辑辅助的默认开关",
       "查找栏默认选项（大小写敏感/全词/正则）记忆",
       "撤销栈深度上限"],
      "BC5 §24（文本编辑视图）+ §32"),

    A("OPT-008", "选项外观", "报表与打印选项",
      "入口：选项 → 报表与打印。\n"
      "作用对象：报表默认值与打印页面默认值。\n"
      "边界：默认编码必须避免 Windows 下 Excel 打开 CSV 乱码。",
      ["默认报表格式与布局、默认输出目录与文件名模板",
       "报表默认编码（Windows 下 CSV 默认 UTF-8 with BOM）与默认包含范围（只含差异）",
       "默认是否内嵌 CSS、是否包含时间戳与 CRC",
       "打印页面默认纸张、方向、边距、页眉页脚内容",
       "报表预设的管理入口"],
      "BC5 §16（报表与打印）+ §32"),

    A("OPT-009", "选项外观", "网络与连接配置选项",
      "入口：选项 → 网络与连接。\n"
      "作用对象：远程数据源的配置与凭据。\n"
      "边界：按 PRD 作为本地闭环之后的远程阶段实现，依赖远程数据源服务与系统凭据库；服务未实现时显示不可用说明，不提供能保存但无法连接的伪入口。凭据不写明文配置。",
      ["管理连接配置（FTP / SFTP / WebDAV / 云存储），符合「连接配置」列表",
       "凭据存入系统凭据库；配置文件中只保存引用标识",
       "网络代理设置（使用系统代理 / 手动指定）",
       "连接超时与重试次数可配置",
       "提供「清除全部已保存凭据」并可确认"],
      "BC5 §27（远程与云服务配置）+ 用户：跨平台凭据已有经验"),

    A("OPT-010", "选项外观", "日志与诊断选项",
      "入口：选项 → 日志与诊断。\n"
      "作用对象：日志级别、日志目录、诊断能力。\n"
      "边界：诊断能力必须在用户机器上可用，"
       "不能要求用户自行配置调试器。",
      ["日志级别（错误/警告/信息/调试）与日志文件位置可配置",
       "日志文件轮转策略（按大小或按天）",
       "提供「打开日志目录」「清空日志」「导出诊断包（日志+版本+环境信息）」",
       "提供「记录详细性能计时」开关，用于排查慢操作",
       "诊断包导出前提示会包含路径信息，并允许脱敏"],
      "BC5 §38（帮助、许可与诊断）+ Ailecium Services/Diagnostics"),

    A("OPT-011", "选项外观", "自定义命令与快捷键管理界面",
      "入口：选项 → 自定义命令；Tools 页 Custom Commands 组。\n"
      "作用对象：用户扩展的命令与键盘绑定。\n"
      "边界：自定义命令必须能引用内置命令的启用条件，"
       "避免出现「永远可用」的错误状态。",
      ["自定义命令可新增/编辑/删除/复制/排序/导入/导出",
       "命令定义包含：名称、说明、程序路径、参数（含变量）、工作目录、图标、适用会话类型",
       "快捷键标签页列出全部命令（内置+自定义）并可修改绑定、检测冲突",
       "提供「导入他人命令与快捷键配置」的合并流程（非覆盖）",
       "自定义命令执行失败时错误信息展示在输出面板"],
      "BC5 §34（自定义命令与快捷键）+ UI-027"),

    A("OPT-012", "选项外观", "设置存储、便携模式与重置",
      "入口：选项 → 存储 / 命令行 `--portable`。\n"
      "作用对象：配置文件的位置与形态。\n"
      "边界：便携模式（配置存于程序目录）与标准模式（配置存于用户目录）"
       "必须互不干扰，且有明确提示。",
      ["标准模式：配置存于用户配置目录（Windows %APPDATA% / macOS ~/Library/Application Support / Linux XDG）",
       "便携模式：可执行文件同目录下存在 `lqcompare.portable` 标记文件时启用，配置存于同目录 `config/`",
       "配置文件为可读文本格式，可手工编辑与版本管理",
       "提供「重置全部设置」「重置某一分类」并支持先导出备份",
       "设置文件的读写全部经过设置仓库，读取失败时回退出厂默认并记录日志"],
      "BC5 §37（设置存储、导入导出与迁移）"),

    A("OPT-013", "选项外观", "全局设置的导入与导出",
      "入口：Application Menu → 导入/导出设置。\n"
      "作用对象：跨机器迁移与团队分发。\n"
      "边界：导出内容不得包含凭据与最近路径等隐私信息（除非用户显式勾选）。",
      ["支持导出全部设置或选定子集（外观、格式定义、快捷键、自定义命令、报表预设）",
       "默认不导出凭据、最近列表、窗口位置等机器相关项",
       "导入时给出清单（新增/冲突/覆盖）并让用户逐项决定",
       "导入为原子操作：中途失败则回滚到导入前状态（先备份现有设置）",
       "导入导出都有日志记录"],
      "BC5 §37 + FMT-011"),

    A("OPT-014", "选项外观", "选项模块的测试",
      "入口：测试工程的选项套件。\n"
      "作用对象：设置仓库、层级解析、导入导出、重置。\n"
      "边界：设置仓库必须可在无界面下测试。",
      ["断言比较设置解析优先级为视图 > 会话 > 类型默认 > 出厂默认；程序选项的比较页只是类型默认入口，不额外增加一层",
       "断言写入设置后重新读取得到相同值（往返一致，含特殊字符与 Unicode）",
       "断言损坏的配置文件被安全忽略并回退出厂默认，且原文件被备份为 .bak",
       "断言导入导出往返一致，且默认不含凭据",
       "断言便携模式与标准模式互不干扰（两类目录分别验证）"],
      "Ailecium Code/Tests 约定"),

    # ------------------------------------------------------------------
    # PLAT —— 平台与性能
    # ------------------------------------------------------------------
    A("PLAT-001", "平台性能", "构建系统与多平台编译",
      "入口：qmake 工程文件与构建脚本。\n"
      "作用对象：Windows（MinGW 32 位为主）与 macOS 的构建。\n"
      "边界：不引入需要付费或破解工具链的依赖；"
       "严格 Qt 5.15.2 + C++17。",
      ["Qt 5.15.2 + C++17，qmake 构建，按模块以 .pri 组织依赖",
       "Windows 上 MinGW 8.1.0 32 位可完整构建出可运行 exe（与 Ailecium 一致）",
       "macOS 上 clang_64 可构建出可运行的 app bundle（用于开发与测试）",
       "构建产物目录与源码目录分离（out-of-source），不把二进制提交进仓库",
       "构建脚本在无 Qt Creator 的环境下也可用（纯命令行 qmake + make）"],
      "Ailecium Code/Ailecium.pro 结构 + 用户 Qt 环境（Qt 5.15.2 clang_64 / MinGW）"),

    A("PLAT-002", "平台性能", "文件系统服务抽象层",
      "入口：Services/Files 与 Services/Platform 模块。\n"
      "作用对象：路径规范化、文件枚举、时间戳、属性、权限。\n"
      "边界：业务代码不得直接调用平台 API 或硬编码路径分隔符，"
       "全部通过抽象层，且抽象层可被假实现替换以测试异常路径。",
      ["定义接口：enumerateDirectory、stat、setTimes、setAttributes、deleteToTrash、pathNormalize",
       "Windows / POSIX 两套实现，公共逻辑提取到平台无关层",
       "路径处理正确区分大小写语义、分隔符、UNC 前缀、长路径前缀",
       "时间戳统一以 UTC 纳秒内部表示，显示层再转本地时间",
       "抽象层有独立单元测试，且异常路径（权限、占用、不存在）用假实现覆盖"],
      "BC5 §36（文件系统细节）+ Ailecium Services/Files"),

    A("PLAT-003", "平台性能", "回收站与可逆删除服务",
      "入口：所有删除操作。\n"
      "作用对象：把删除变为可恢复的操作。\n"
      "边界：回收站不可用时必须明确告知并让用户选择（取消 / 永久删除），"
       "绝不静默降级为永久删除。",
      ["Windows 使用 SHFileOperation / IFileOperation 删除到回收站（支持显示进度与撤销）",
       "macOS 通过移动到废纸篓（NSFileManager trashItem）",
       "Linux 遵循 XDG 规范（~/.local/share/Trash）或调用 gio trash",
       "回收站不可用（网络盘、空间不足、超出回收站配额）时明确提示并让用户选择",
       "提供「从回收站还原最近一次删除」的能力（受平台能力限制时说明）"],
      "BC5 §10（删除）+ 用户要求：删除必须可逆"),

    A("PLAT-004", "平台性能", "系统图标与文件类型识别服务",
      "入口：文件夹树与列表的条目图标。\n"
      "作用对象：按文件类型显示系统图标。\n"
      "边界：图标获取必须缓存并异步，"
       "否则在大目录下会因逐个系统调用而严重变慢。",
      ["按扩展名获取系统关联图标（Windows SHGetFileInfo / macOS NSWorkspace / Linux 主题图标）",
       "图标缓存按扩展名而非按文件，缓存命中率高",
       "图标获取在后台线程，缺失时回退到内置的通用图标",
       "系统图标不可用时（无桌面环境）回退到内置图标集，不崩溃",
       "图标大小随 DPI 与界面缩放正确获取"],
      "TGIT §B（Shell 集成）+ BC5 §33（外观）"),

    A("PLAT-005", "平台性能", "Shell 集成（右键菜单与文件关联）",
      "入口：安装时的可选 Shell 集成。\n"
      "作用对象：从系统文件管理器直接调用本工具。\n"
      "边界：Shell 集成必须可在安装/选项中被完全关闭，"
       "并且不残留注册表项。",
      ["Windows：提供「比较」「与…比较」「作为左侧/右侧比较」等右键菜单项，通过注册表实现",
       "提供「选择第二个文件后比较」的两步式交互（占位菜单项）",
       "文件关联可注册 .patch/.diff 等扩展名，可单独关闭",
       "全部集成项支持一键安装与一键卸载，卸载后注册表无残留（有校验）",
       "非 Windows 平台该能力置灰并说明"],
      "TGIT §B（资源管理器右键菜单所有 diff 相关项）"),

    A("PLAT-006", "平台性能", "单实例与进程间通信",
      "入口：应用第二次启动。\n"
      "作用对象：参数转发与窗口激活。\n"
      "边界：进程间通信必须跨平台且有超时保护；"
       "通信失败时降级为启动新实例，绝不让用户看到失败。",
      ["实现跨平台单实例机制（共享内存 + 本地套接字 / QLocalServer）",
       "第二个实例把参数转发给首个实例并退出，退出码反映转发结果",
       "收到参数后首个实例创建会话并把窗口置前（不抢焦点导致用户中断输入时可配置）",
       "首个实例无响应时（超时）第二个实例自行启动并记录日志",
       "单实例行为可由选项关闭（见 OPT-002）"],
      "BC5 §29.5 + PLAT-005"),

    A("PLAT-007", "平台性能", "Unicode、长路径与特殊文件名",
      "入口：处理非 ASCII 路径、超长路径、含特殊字符的名称。\n"
      "作用对象：路径处理的健壮性。\n"
      "边界：中文路径不乱码是最低要求；"
       "任何路径相关字符串转换不得有精度损失或异常。",
      ["全链路使用 UTF-16/UTF-8 正确转换，Windows 上使用宽字符 API（不经过 ANSI 转换）",
       "Win11 下通过 `\\\\?\\` 前缀支持超过 260 字符的路径",
       "文件名含引号、换行、制表符、前后空格、Unicode 组合字符时正确显示与操作",
       "非法文件名字符在重命名/新建时被统一校验并给出明确原因",
       "Linux 上含无效 UTF-8 字节的文件名以替代方式显示但不丢失（可原样操作）"],
      "BC5 §36（长路径与 Unicode）+ Ailecium 的路径处理经验"),

    A("PLAT-008", "平台性能", "权限、只读与文件占用处理",
      "入口：写操作遇到权限不足或文件被占用。\n"
      "作用对象：错误分类与用户可操作性。\n"
      "边界：必须区分「无权限」「只读」「被占用」「只读文件系统」四类，"
       "并给出各自不同的建议，不能统一报「操作失败」。",
      ["四类错误分别识别并给出不同提示与建议（提权/解除只读/关闭占用程序/换目标目录）",
       "批量操作中被占用/无权限的条目汇总为失败清单，可单独重试",
       "提供「重试失败项」与「跳过并继续」两条出路",
       "长任务中途出现错误时不中断整体（保持已完成的进度）",
       "错误信息包含原始系统错误码便于排查"],
      "BC5 §36（权限与所有者）+ 用户要求：错误必须可定位"),

    A("PLAT-009", "平台性能", "性能基准与回归护栏",
      "入口：性能基准套件。\n"
      "作用对象：关键路径的耗时与内存。\n"
      "边界：基准必须可重复（固定语料、固定环境提示）并且有阈值断言，"
       "否则性能会随迭代慢慢劣化。",
      ["基准覆盖：文本比对算法（1 万行/10 万行）、目录扫描（10 万条目）、CRC 计算（1GB）、报表生成（10 万条）",
       "每个基准有耗时与峰值内存断言，且阈值在 CI 中作为失败条件",
       "基准在固定语料上运行且结果可输出为 CSV 便于趋势对比",
       "基准可单独运行（不需要全量测试）",
       "基准代码与产品代码共享同一实现路径（不测仿制品）"],
      "BC5 §36（性能选项）+ Ailecium docs/development/test-runner.md"),

    A("PLAT-010", "平台性能", "平台差异的测试矩阵",
      "入口：CI 与本地测试运行。\n"
      "作用对象：跨平台行为一致性。\n"
      "边界：平台相关的跳过必须有明确原因，"
       "不能以「在本平台跑不过」为由长期跳过。",
      ["测试在 macOS 与 Windows 上均运行，平台不适用项显式 skip 并附原因",
       "路径分隔符、换行、大小写敏感性、时间戳精度的差异有专门用例覆盖",
       "offscreen 平台下可运行全部界面逻辑测试（用于无显示器环境）",
       "CI 记录各平台测试结果矩阵，失败可定位到具体平台",
       "提供「平台差异清单」文档，列出已知有意为之的行为差异"],
      "Ailecium docs/development/cross-platform-*.md 系列经验"),
]
